# /docs Help Center Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Replace the placeholder `/docs` page with a full, consumer-facing help center covering every verified feature of Douxue — 12 categories × 48 topics, statically prerendered, one typed registry as the single source of truth.

**Architecture:** Multi-page dynamic routing under `/docs/[category]/[topic]`, driven by a TypeScript registry in `src/features/web/docs/registry.ts`. Every route is a server component; the only client boundaries are the sidebar highlight (`usePathname`), the mobile drawer trigger, the right-rail scroll-spy TOC, and the FAQ accordion. Content is composed from 12 reusable visual primitives. All 61 pages are statically prerendered via `generateStaticParams` — zero runtime API calls, works with the backend offline.

**Tech Stack:** Next.js 16 (App Router, React Server Components), TypeScript strict mode, TailwindCSS v4 (oklch theme), shadcn/ui New York (already installed — `accordion` and `sheet` present), Lucide React icons. No new npm dependencies.

**Reference spec:** `docs/superpowers/specs/2026-04-13-docs-page-design.md` — all IA, primitives, and content outlines live there. Each content task cites specific spec line ranges.

**Working directory:** All paths in this plan are relative to `dx-web/` unless otherwise noted. Run all shell commands from `dx-web/` (`cd dx-web` first).

**Commit style:** One commit per task. Prefixes: `feat:` (new files), `refactor:` (restructuring), `chore:` (tooling), `docs:` (spec/plan changes).

**Per-task verification:** After each task that creates or edits TS/TSX files, run `cd dx-web && npx tsc --noEmit 2>&1 | tail -20` to catch TypeScript errors. Expected: zero output (success). Full `npm run build` + `npm run lint` happens once at Task 23.

**Scope note:** LandingHeader already has a `/docs` link, so the primary entry point is already live. The footer link columns are mock UI (plain spans, no hrefs) — out of scope. The hall sidebar menu is backend-driven via `useHallMenu()` — adding 帮助中心 there requires a backend menu change and is also out of scope for this plan.

---

## Task 1: Create docs types and empty registry

**Files:**
- Create: `src/features/web/docs/types.ts`
- Create: `src/features/web/docs/registry.ts`

- [ ] **Step 1: Create the types file**

Create `src/features/web/docs/types.ts`:

```ts
import type { ComponentType } from "react";
import type { LucideIcon } from "lucide-react";

export type DocTopic = {
  slug: string;
  title: string;
  description: string;
  Component: ComponentType;
};

export type DocCategory = {
  slug: string;
  title: string;
  description: string;
  icon: LucideIcon;
  accentClass: string;
  topics: DocTopic[];
};

export type TopicRef = {
  category: DocCategory;
  topic: DocTopic;
};
```

- [ ] **Step 2: Create the empty registry with helpers**

Create `src/features/web/docs/registry.ts`:

```ts
import type { DocCategory, TopicRef } from "./types";

// Categories are added incrementally by content tasks (11-22).
export const DOC_CATEGORIES: DocCategory[] = [];

export function findCategory(slug: string): DocCategory | undefined {
  return DOC_CATEGORIES.find((c) => c.slug === slug);
}

export function flatTopics(): TopicRef[] {
  return DOC_CATEGORIES.flatMap((category) =>
    category.topics.map((topic) => ({ category, topic })),
  );
}

export function findTopic(
  catSlug: string,
  topicSlug: string,
):
  | { ref: TopicRef; prev: TopicRef | null; next: TopicRef | null }
  | undefined {
  const flat = flatTopics();
  const index = flat.findIndex(
    ({ category, topic }) =>
      category.slug === catSlug && topic.slug === topicSlug,
  );
  if (index === -1) return undefined;
  return {
    ref: flat[index],
    prev: index > 0 ? flat[index - 1] : null,
    next: index < flat.length - 1 ? flat[index + 1] : null,
  };
}
```

- [ ] **Step 3: Typecheck**

Run: `cd dx-web && npx tsc --noEmit 2>&1 | tail -20`
Expected: zero output.

- [ ] **Step 4: Commit**

```bash
cd dx-web && git add src/features/web/docs/types.ts src/features/web/docs/registry.ts && git commit -m "feat(docs): add types and empty registry for /docs help center"
```

---

## Task 2: Create primitives batch A — DocSection, DocCallout, DocSteps, DocFeatureGrid

**Files:**
- Create: `src/features/web/docs/primitives/doc-section.tsx`
- Create: `src/features/web/docs/primitives/doc-callout.tsx`
- Create: `src/features/web/docs/primitives/doc-steps.tsx`
- Create: `src/features/web/docs/primitives/doc-feature-grid.tsx`

- [ ] **Step 1: Create DocSection**

Create `src/features/web/docs/primitives/doc-section.tsx`:

```tsx
import type { ReactNode } from "react";

type Props = {
  id: string;
  title: string;
  children: ReactNode;
};

export function DocSection({ id, title, children }: Props) {
  return (
    <section id={id} className="flex scroll-mt-24 flex-col gap-4">
      <h2 className="text-xl font-bold tracking-tight text-slate-900 md:text-2xl">
        {title}
      </h2>
      <div className="flex flex-col gap-4 text-[15px] leading-[1.7] text-slate-600">
        {children}
      </div>
    </section>
  );
}
```

- [ ] **Step 2: Create DocCallout**

Create `src/features/web/docs/primitives/doc-callout.tsx`:

```tsx
import {
  AlertTriangle,
  CheckCircle2,
  Info,
  Lightbulb,
  type LucideIcon,
} from "lucide-react";
import type { ReactNode } from "react";

type Variant = "info" | "tip" | "warning" | "success";

const VARIANTS: Record<
  Variant,
  {
    icon: LucideIcon;
    boxClass: string;
    titleClass: string;
    bodyClass: string;
  }
> = {
  info: {
    icon: Info,
    boxClass: "border-teal-200 bg-teal-50",
    titleClass: "text-teal-700",
    bodyClass: "text-teal-700",
  },
  tip: {
    icon: Lightbulb,
    boxClass: "border-amber-200 bg-amber-50",
    titleClass: "text-amber-700",
    bodyClass: "text-amber-700",
  },
  warning: {
    icon: AlertTriangle,
    boxClass: "border-rose-200 bg-rose-50",
    titleClass: "text-rose-700",
    bodyClass: "text-rose-700",
  },
  success: {
    icon: CheckCircle2,
    boxClass: "border-emerald-200 bg-emerald-50",
    titleClass: "text-emerald-700",
    bodyClass: "text-emerald-700",
  },
};

type Props = {
  variant?: Variant;
  title?: string;
  children: ReactNode;
};

export function DocCallout({ variant = "info", title, children }: Props) {
  const { icon: Icon, boxClass, titleClass, bodyClass } = VARIANTS[variant];
  return (
    <div className={`flex gap-3 rounded-lg border p-4 ${boxClass}`}>
      <Icon className={`h-5 w-5 shrink-0 ${titleClass}`} />
      <div className="flex flex-col gap-1">
        {title && (
          <span className={`text-[13px] font-semibold ${titleClass}`}>
            {title}
          </span>
        )}
        <div className={`text-sm leading-snug ${bodyClass}`}>{children}</div>
      </div>
    </div>
  );
}
```

- [ ] **Step 3: Create DocSteps**

Create `src/features/web/docs/primitives/doc-steps.tsx`:

```tsx
type Step = { title: string; desc: string };

type Props = { steps: Step[] };

export function DocSteps({ steps }: Props) {
  return (
    <div className="flex flex-col gap-3">
      {steps.map((step, i) => (
        <div key={i} className="flex gap-3">
          <div className="flex h-7 w-7 shrink-0 items-center justify-center rounded-full bg-teal-600">
            <span className="text-xs font-bold text-white">{i + 1}</span>
          </div>
          <div className="flex flex-col gap-1">
            <span className="text-sm font-semibold text-slate-900">
              {step.title}
            </span>
            <span className="text-[13px] text-slate-500">{step.desc}</span>
          </div>
        </div>
      ))}
    </div>
  );
}
```

- [ ] **Step 4: Create DocFeatureGrid**

Create `src/features/web/docs/primitives/doc-feature-grid.tsx`:

```tsx
import type { LucideIcon } from "lucide-react";

type Item = {
  icon: LucideIcon;
  iconColor: string;
  title: string;
  desc: string;
};

type Props = {
  columns?: 2 | 3 | 4;
  items: Item[];
};

export function DocFeatureGrid({ columns = 3, items }: Props) {
  const colsClass =
    columns === 2
      ? "lg:grid-cols-2"
      : columns === 4
        ? "lg:grid-cols-4"
        : "lg:grid-cols-3";
  return (
    <div className={`grid grid-cols-1 gap-4 sm:grid-cols-2 ${colsClass}`}>
      {items.map((item, i) => {
        const Icon = item.icon;
        return (
          <div
            key={i}
            className="flex flex-col gap-2.5 rounded-[10px] border border-slate-200 bg-white p-5"
          >
            <Icon className={`h-7 w-7 ${item.iconColor}`} />
            <span className="text-[15px] font-semibold text-slate-900">
              {item.title}
            </span>
            <span className="text-[13px] leading-snug text-slate-500">
              {item.desc}
            </span>
          </div>
        );
      })}
    </div>
  );
}
```

- [ ] **Step 5: Typecheck**

Run: `cd dx-web && npx tsc --noEmit 2>&1 | tail -20`
Expected: zero output.

- [ ] **Step 6: Commit**

```bash
cd dx-web && git add src/features/web/docs/primitives/ && git commit -m "feat(docs): add primitives batch A (section/callout/steps/feature-grid)"
```

---

## Task 3: Create primitives batch B — DocCompareTable, DocKeyValue, DocFlow

**Files:**
- Create: `src/features/web/docs/primitives/doc-compare-table.tsx`
- Create: `src/features/web/docs/primitives/doc-key-value.tsx`
- Create: `src/features/web/docs/primitives/doc-flow.tsx`

- [ ] **Step 1: Create DocCompareTable**

Create `src/features/web/docs/primitives/doc-compare-table.tsx`:

```tsx
import { Check, X } from "lucide-react";
import type { ReactNode } from "react";

type Cell = boolean | string | ReactNode;

type Row = {
  label: string;
  values: Cell[];
};

type Props = {
  columns: string[];
  rows: Row[];
  labelHeader?: string;
};

export function DocCompareTable({
  columns,
  rows,
  labelHeader = "功能 / 权益",
}: Props) {
  return (
    <div className="overflow-x-auto rounded-[10px] border border-slate-200">
      <table className="w-full text-sm">
        <thead>
          <tr className="border-b border-slate-200 bg-slate-50">
            <th className="px-4 py-3 text-left font-semibold text-slate-700">
              {labelHeader}
            </th>
            {columns.map((col) => (
              <th
                key={col}
                className="px-4 py-3 text-center font-semibold text-slate-700"
              >
                {col}
              </th>
            ))}
          </tr>
        </thead>
        <tbody>
          {rows.map((row, i) => (
            <tr
              key={i}
              className="border-b border-slate-100 last:border-b-0"
            >
              <td className="px-4 py-3 font-medium text-slate-700">
                {row.label}
              </td>
              {row.values.map((v, j) => (
                <td
                  key={j}
                  className="px-4 py-3 text-center text-slate-600"
                >
                  {typeof v === "boolean" ? (
                    v ? (
                      <Check className="mx-auto h-4 w-4 text-teal-600" />
                    ) : (
                      <X className="mx-auto h-4 w-4 text-slate-300" />
                    )
                  ) : (
                    v
                  )}
                </td>
              ))}
            </tr>
          ))}
        </tbody>
      </table>
    </div>
  );
}
```

- [ ] **Step 2: Create DocKeyValue**

Create `src/features/web/docs/primitives/doc-key-value.tsx`:

```tsx
type Item = {
  key: string;
  value: string;
  note?: string;
};

type Props = { items: Item[] };

export function DocKeyValue({ items }: Props) {
  return (
    <div className="rounded-[10px] border border-slate-200 bg-white">
      {items.map((item, i) => (
        <div
          key={i}
          className={`flex flex-col gap-1 px-4 py-3 sm:flex-row sm:items-center sm:justify-between ${
            i < items.length - 1 ? "border-b border-slate-100" : ""
          }`}
        >
          <span className="text-sm font-medium text-slate-700">
            {item.key}
          </span>
          <span className="text-sm text-slate-600">
            {item.value}
            {item.note && (
              <span className="ml-2 text-xs text-slate-400">{item.note}</span>
            )}
          </span>
        </div>
      ))}
    </div>
  );
}
```

- [ ] **Step 3: Create DocFlow**

Create `src/features/web/docs/primitives/doc-flow.tsx`:

```tsx
import { ChevronRight } from "lucide-react";
import { Fragment } from "react";

type Node = {
  label: string;
  desc?: string;
};

type Props = { nodes: Node[] };

export function DocFlow({ nodes }: Props) {
  return (
    <div className="flex flex-col gap-3 rounded-[10px] border border-slate-200 bg-slate-50 p-4 sm:flex-row sm:items-stretch">
      {nodes.map((node, i) => (
        <Fragment key={i}>
          <div className="flex flex-1 flex-col gap-1 rounded-md bg-white px-3 py-3 text-center">
            <span className="text-sm font-semibold text-slate-900">
              {node.label}
            </span>
            {node.desc && (
              <span className="text-xs text-slate-500">{node.desc}</span>
            )}
          </div>
          {i < nodes.length - 1 && (
            <div className="flex items-center justify-center sm:px-1">
              <ChevronRight className="h-5 w-5 rotate-90 text-slate-400 sm:rotate-0" />
            </div>
          )}
        </Fragment>
      ))}
    </div>
  );
}
```

- [ ] **Step 4: Typecheck**

Run: `cd dx-web && npx tsc --noEmit 2>&1 | tail -20`
Expected: zero output.

- [ ] **Step 5: Commit**

```bash
cd dx-web && git add src/features/web/docs/primitives/doc-compare-table.tsx src/features/web/docs/primitives/doc-key-value.tsx src/features/web/docs/primitives/doc-flow.tsx && git commit -m "feat(docs): add primitives batch B (compare-table/key-value/flow)"
```

---

## Task 4: Create primitives batch C — DocSlug, DocLink, DocPseudoUI, DocCodeBlock

**Files:**
- Create: `src/features/web/docs/primitives/doc-slug.tsx`
- Create: `src/features/web/docs/primitives/doc-link.tsx`
- Create: `src/features/web/docs/primitives/doc-pseudo-ui.tsx`
- Create: `src/features/web/docs/primitives/doc-code-block.tsx`

- [ ] **Step 1: Create DocSlug**

Create `src/features/web/docs/primitives/doc-slug.tsx`:

```tsx
import type { ReactNode } from "react";

export function DocSlug({ children }: { children: ReactNode }) {
  return (
    <code className="rounded bg-slate-100 px-1.5 py-0.5 font-mono text-[13px] text-slate-700">
      {children}
    </code>
  );
}
```

- [ ] **Step 2: Create DocLink**

Create `src/features/web/docs/primitives/doc-link.tsx`:

```tsx
import Link from "next/link";
import { ExternalLink } from "lucide-react";
import type { ReactNode } from "react";

type Props = {
  href: string;
  external?: boolean;
  children: ReactNode;
};

export function DocLink({ href, external, children }: Props) {
  const className =
    "inline-flex items-center gap-1 text-teal-600 underline underline-offset-2 hover:text-teal-700";
  if (external) {
    return (
      <a
        href={href}
        target="_blank"
        rel="noopener noreferrer"
        className={className}
      >
        {children}
        <ExternalLink className="h-3.5 w-3.5" />
      </a>
    );
  }
  return (
    <Link href={href} className={className}>
      {children}
    </Link>
  );
}
```

- [ ] **Step 3: Create DocPseudoUI**

Create `src/features/web/docs/primitives/doc-pseudo-ui.tsx`:

```tsx
import type { ReactNode } from "react";

type Props = {
  label?: string;
  children: ReactNode;
};

export function DocPseudoUI({ label, children }: Props) {
  return (
    <div className="relative rounded-[10px] border border-slate-200 bg-slate-50 p-5">
      {label && (
        <span className="absolute -top-2.5 left-4 rounded bg-white px-2 text-[11px] font-medium text-slate-500">
          {label}
        </span>
      )}
      <div className="flex flex-col gap-2 text-sm text-slate-700">
        {children}
      </div>
    </div>
  );
}
```

- [ ] **Step 4: Create DocCodeBlock**

Create `src/features/web/docs/primitives/doc-code-block.tsx`:

```tsx
type Props = {
  children: string;
};

export function DocCodeBlock({ children }: Props) {
  return (
    <pre className="overflow-x-auto rounded-lg bg-slate-800 px-5 py-4">
      <code className="font-mono text-[13px] text-slate-200">{children}</code>
    </pre>
  );
}
```

- [ ] **Step 5: Typecheck**

Run: `cd dx-web && npx tsc --noEmit 2>&1 | tail -20`
Expected: zero output.

- [ ] **Step 6: Commit**

```bash
cd dx-web && git add src/features/web/docs/primitives/doc-slug.tsx src/features/web/docs/primitives/doc-link.tsx src/features/web/docs/primitives/doc-pseudo-ui.tsx src/features/web/docs/primitives/doc-code-block.tsx && git commit -m "feat(docs): add primitives batch C (slug/link/pseudo-ui/code-block)"
```

---

## Task 5: Create DocFaqAccordion (client primitive)

**Files:**
- Create: `src/features/web/docs/primitives/doc-faq-accordion.tsx`

- [ ] **Step 1: Create DocFaqAccordion**

Create `src/features/web/docs/primitives/doc-faq-accordion.tsx`:

```tsx
"use client";

import {
  Accordion,
  AccordionContent,
  AccordionItem,
  AccordionTrigger,
} from "@/components/ui/accordion";

type Item = {
  question: string;
  answer: string;
};

type Props = {
  items: Item[];
};

export function DocFaqAccordion({ items }: Props) {
  return (
    <Accordion type="single" collapsible className="w-full">
      {items.map((item, i) => (
        <AccordionItem key={i} value={`item-${i}`}>
          <AccordionTrigger className="text-left text-[15px] font-medium text-slate-800">
            {item.question}
          </AccordionTrigger>
          <AccordionContent className="text-sm leading-[1.7] text-slate-600">
            {item.answer}
          </AccordionContent>
        </AccordionItem>
      ))}
    </Accordion>
  );
}
```

- [ ] **Step 2: Typecheck**

Run: `cd dx-web && npx tsc --noEmit 2>&1 | tail -20`
Expected: zero output.

- [ ] **Step 3: Commit**

```bash
cd dx-web && git add src/features/web/docs/primitives/doc-faq-accordion.tsx && git commit -m "feat(docs): add DocFaqAccordion primitive"
```

---

## Task 6: Create shell components — DocsSidebar, DocsSidebarDrawer, DocsToc

**Files:**
- Create: `src/features/web/docs/components/docs-sidebar.tsx`
- Create: `src/features/web/docs/components/docs-sidebar-drawer.tsx`
- Create: `src/features/web/docs/components/docs-toc.tsx`

- [ ] **Step 1: Create DocsSidebar (client component with `usePathname` highlight)**

Create `src/features/web/docs/components/docs-sidebar.tsx`:

```tsx
"use client";

import Link from "next/link";
import { usePathname } from "next/navigation";
import { DOC_CATEGORIES } from "@/features/web/docs/registry";

export function DocsSidebar() {
  const pathname = usePathname();

  return (
    <div className="flex flex-col gap-1">
      <Link
        href="/docs"
        className="mb-3 text-sm font-extrabold tracking-tight text-slate-900 hover:text-teal-600"
      >
        斗学文档
      </Link>
      {DOC_CATEGORIES.map((category, ci) => {
        const catHref = `/docs/${category.slug}`;
        const isActiveCat = pathname?.startsWith(catHref) ?? false;
        const CatIcon = category.icon;
        return (
          <div key={category.slug}>
            {ci > 0 && <div className="my-2 h-px w-full bg-slate-200" />}
            <Link
              href={catHref}
              className={`flex items-center gap-2 rounded-md px-3 py-2 text-sm font-semibold ${
                isActiveCat ? "text-teal-600" : "text-slate-900"
              }`}
            >
              <CatIcon
                className={`h-4 w-4 ${
                  isActiveCat ? "text-teal-600" : "text-slate-400"
                }`}
              />
              {category.title}
            </Link>
            <div className="mt-1 flex flex-col gap-1">
              {category.topics.map((topic) => {
                const topicHref = `/docs/${category.slug}/${topic.slug}`;
                const isActiveTopic = pathname === topicHref;
                return (
                  <Link
                    key={topic.slug}
                    href={topicHref}
                    className={`flex items-center gap-2 rounded-md px-3 py-2 text-left text-sm ${
                      isActiveTopic
                        ? "bg-teal-50 font-medium text-teal-600"
                        : "text-slate-500 hover:text-slate-700"
                    }`}
                  >
                    <div
                      className={`h-5 w-[3px] rounded-sm ${
                        isActiveTopic ? "bg-teal-600" : "bg-transparent"
                      }`}
                    />
                    {topic.title}
                  </Link>
                );
              })}
            </div>
          </div>
        );
      })}
    </div>
  );
}
```

- [ ] **Step 2: Create DocsSidebarDrawer (mobile)**

Create `src/features/web/docs/components/docs-sidebar-drawer.tsx`:

```tsx
"use client";

import { Menu } from "lucide-react";
import {
  Sheet,
  SheetContent,
  SheetHeader,
  SheetTitle,
  SheetTrigger,
} from "@/components/ui/sheet";
import { DocsSidebar } from "./docs-sidebar";

export function DocsSidebarDrawer() {
  return (
    <Sheet>
      <SheetTrigger className="flex items-center gap-1.5 text-sm font-medium text-slate-700 hover:text-slate-900">
        <Menu className="h-4 w-4" />
        目录
      </SheetTrigger>
      <SheetContent side="left" className="w-[280px] overflow-y-auto p-5">
        <SheetHeader>
          <SheetTitle className="text-left text-sm font-extrabold">
            斗学文档
          </SheetTitle>
        </SheetHeader>
        <div className="mt-4">
          <DocsSidebar />
        </div>
      </SheetContent>
    </Sheet>
  );
}
```

- [ ] **Step 3: Create DocsToc (right-rail scroll-spy, client)**

Create `src/features/web/docs/components/docs-toc.tsx`:

```tsx
"use client";

import { useEffect, useState } from "react";

type Heading = {
  id: string;
  label: string;
};

export function DocsToc() {
  const [headings, setHeadings] = useState<Heading[]>([]);
  const [activeId, setActiveId] = useState<string | null>(null);

  useEffect(() => {
    const h2s = Array.from(
      document.querySelectorAll<HTMLHeadingElement>("main h2[id]"),
    );
    const hs: Heading[] = h2s.map((h) => ({
      id: h.id,
      label: h.textContent ?? "",
    }));
    setHeadings(hs);

    if (hs.length === 0) return;

    const observer = new IntersectionObserver(
      (entries) => {
        const visible = entries
          .filter((e) => e.isIntersecting)
          .sort(
            (a, b) => a.boundingClientRect.top - b.boundingClientRect.top,
          );
        if (visible.length > 0) {
          setActiveId(visible[0].target.id);
        }
      },
      { rootMargin: "0px 0px -70% 0px", threshold: 0 },
    );

    h2s.forEach((h) => observer.observe(h));
    return () => observer.disconnect();
  }, []);

  if (headings.length === 0) return null;

  return (
    <aside className="hidden w-[220px] shrink-0 border-l border-slate-200 px-5 py-10 xl:block">
      <div className="sticky top-4 flex flex-col gap-3">
        <span className="text-xs font-semibold tracking-wide text-slate-900">
          本页目录
        </span>
        <div className="mt-1 flex flex-col gap-3">
          {headings.map((h) => (
            <a
              key={h.id}
              href={`#${h.id}`}
              className="flex items-center gap-2 text-left"
            >
              <div
                className={`h-4 w-0.5 rounded-sm ${
                  activeId === h.id ? "bg-teal-600" : "bg-transparent"
                }`}
              />
              <span
                className={`text-[13px] ${
                  activeId === h.id
                    ? "font-medium text-teal-600"
                    : "text-slate-500"
                }`}
              >
                {h.label}
              </span>
            </a>
          ))}
        </div>
      </div>
    </aside>
  );
}
```

- [ ] **Step 4: Typecheck**

Run: `cd dx-web && npx tsc --noEmit 2>&1 | tail -20`
Expected: zero output.

- [ ] **Step 5: Commit**

```bash
cd dx-web && git add src/features/web/docs/components/docs-sidebar.tsx src/features/web/docs/components/docs-sidebar-drawer.tsx src/features/web/docs/components/docs-toc.tsx && git commit -m "feat(docs): add sidebar, mobile drawer, and right-rail TOC"
```

---

## Task 7: Create navigation components — DocsBreadcrumb, DocsPrevNext

**Files:**
- Create: `src/features/web/docs/components/docs-breadcrumb.tsx`
- Create: `src/features/web/docs/components/docs-prev-next.tsx`

- [ ] **Step 1: Create DocsBreadcrumb**

Create `src/features/web/docs/components/docs-breadcrumb.tsx`:

```tsx
import Link from "next/link";
import type { DocCategory, DocTopic } from "@/features/web/docs/types";

type Props = {
  category: DocCategory;
  topic?: DocTopic;
};

export function DocsBreadcrumb({ category, topic }: Props) {
  return (
    <div className="flex items-center gap-2 text-[13px]">
      <Link href="/docs" className="text-slate-400 hover:text-slate-600">
        文档
      </Link>
      <span className="text-slate-300">/</span>
      {topic ? (
        <>
          <Link
            href={`/docs/${category.slug}`}
            className="text-slate-400 hover:text-slate-600"
          >
            {category.title}
          </Link>
          <span className="text-slate-300">/</span>
          <span className="font-medium text-slate-900">{topic.title}</span>
        </>
      ) : (
        <span className="font-medium text-slate-900">{category.title}</span>
      )}
    </div>
  );
}
```

- [ ] **Step 2: Create DocsPrevNext**

Create `src/features/web/docs/components/docs-prev-next.tsx`:

```tsx
import Link from "next/link";
import { ChevronLeft, ChevronRight } from "lucide-react";
import type { TopicRef } from "@/features/web/docs/types";

type Props = {
  prev: TopicRef | null;
  next: TopicRef | null;
};

export function DocsPrevNext({ prev, next }: Props) {
  return (
    <div className="flex items-stretch justify-between gap-4">
      {prev ? (
        <Link
          href={`/docs/${prev.category.slug}/${prev.topic.slug}`}
          className="flex flex-col items-start gap-1 rounded-lg border border-slate-200 px-4 py-3 hover:border-slate-300"
        >
          <span className="text-xs text-slate-400">上一页</span>
          <div className="flex items-center gap-1.5">
            <ChevronLeft className="h-3.5 w-3.5 text-slate-500" />
            <span className="text-sm font-medium text-slate-700">
              {prev.topic.title}
            </span>
          </div>
        </Link>
      ) : (
        <div />
      )}

      {next ? (
        <Link
          href={`/docs/${next.category.slug}/${next.topic.slug}`}
          className="flex flex-col items-end gap-1 rounded-lg border border-slate-200 px-4 py-3 hover:border-slate-300"
        >
          <span className="text-xs text-slate-400">下一页</span>
          <div className="flex items-center gap-1.5">
            <span className="text-sm font-medium text-slate-700">
              {next.topic.title}
            </span>
            <ChevronRight className="h-3.5 w-3.5 text-slate-500" />
          </div>
        </Link>
      ) : (
        <div />
      )}
    </div>
  );
}
```

- [ ] **Step 3: Typecheck**

Run: `cd dx-web && npx tsc --noEmit 2>&1 | tail -20`
Expected: zero output.

- [ ] **Step 4: Commit**

```bash
cd dx-web && git add src/features/web/docs/components/docs-breadcrumb.tsx src/features/web/docs/components/docs-prev-next.tsx && git commit -m "feat(docs): add breadcrumb and prev/next navigation components"
```

---

## Task 8: Create landing page components — DocsHomeHero, DocsCategoryGrid, DocsCategoryIndex

**Files:**
- Create: `src/features/web/docs/components/docs-home-hero.tsx`
- Create: `src/features/web/docs/components/docs-category-grid.tsx`
- Create: `src/features/web/docs/components/docs-category-index.tsx`

- [ ] **Step 1: Create DocsHomeHero**

Create `src/features/web/docs/components/docs-home-hero.tsx`:

```tsx
import Link from "next/link";

export function DocsHomeHero() {
  return (
    <div className="flex flex-col gap-4">
      <h1 className="text-2xl font-extrabold tracking-tight text-slate-900 md:text-4xl">
        斗学帮助中心
      </h1>
      <p className="text-base leading-relaxed text-slate-500">
        了解每一项功能的详细用法，让学习更顺畅。
      </p>
      <div className="flex flex-wrap gap-3">
        <Link
          href="/docs/getting-started/what-is-douxue"
          className="inline-flex items-center gap-2 rounded-lg bg-teal-600 px-4 py-2 text-sm font-semibold text-white hover:bg-teal-700"
        >
          快速开始
        </Link>
        <Link
          href="/docs/account/faq"
          className="inline-flex items-center gap-2 rounded-lg border border-slate-200 bg-white px-4 py-2 text-sm font-medium text-slate-700 hover:bg-slate-50"
        >
          常见问题
        </Link>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: Create DocsCategoryGrid**

Create `src/features/web/docs/components/docs-category-grid.tsx`:

```tsx
import Link from "next/link";
import { DOC_CATEGORIES } from "@/features/web/docs/registry";

export function DocsCategoryGrid() {
  return (
    <div className="flex flex-col gap-4">
      <h2 className="text-xl font-bold tracking-tight text-slate-900 md:text-2xl">
        全部分类
      </h2>
      <div className="grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-3">
        {DOC_CATEGORIES.map((category) => {
          const Icon = category.icon;
          return (
            <Link
              key={category.slug}
              href={`/docs/${category.slug}`}
              className="flex flex-col gap-2.5 rounded-[10px] border border-slate-200 bg-white p-5 hover:border-slate-300"
            >
              <div
                className={`flex h-10 w-10 items-center justify-center rounded-lg border ${category.accentClass}`}
              >
                <Icon className="h-5 w-5" />
              </div>
              <span className="text-[15px] font-semibold text-slate-900">
                {category.title}
              </span>
              <span className="text-[13px] leading-snug text-slate-500">
                {category.description}
              </span>
              <span className="mt-auto pt-2 text-[12px] text-slate-400">
                {category.topics.length} 个主题 →
              </span>
            </Link>
          );
        })}
      </div>
    </div>
  );
}
```

- [ ] **Step 3: Create DocsCategoryIndex**

Create `src/features/web/docs/components/docs-category-index.tsx`:

```tsx
import Link from "next/link";
import { ChevronRight } from "lucide-react";
import { DocsBreadcrumb } from "./docs-breadcrumb";
import type { DocCategory } from "@/features/web/docs/types";

type Props = { category: DocCategory };

export function DocsCategoryIndex({ category }: Props) {
  const Icon = category.icon;
  return (
    <>
      <DocsBreadcrumb category={category} />
      <div className="flex flex-col gap-3">
        <div className="flex items-center gap-3">
          <div
            className={`flex h-12 w-12 items-center justify-center rounded-lg border ${category.accentClass}`}
          >
            <Icon className="h-6 w-6" />
          </div>
          <h1 className="text-2xl font-extrabold tracking-tight text-slate-900 md:text-4xl">
            {category.title}
          </h1>
        </div>
        <p className="text-base leading-relaxed text-slate-500">
          {category.description}
        </p>
      </div>
      <div className="h-px w-full bg-slate-200" />
      <div className="flex flex-col gap-3">
        {category.topics.map((topic, i) => (
          <Link
            key={topic.slug}
            href={`/docs/${category.slug}/${topic.slug}`}
            className="group flex items-center gap-4 rounded-[10px] border border-slate-200 bg-white p-5 hover:border-slate-300"
          >
            <div className="flex h-8 w-8 shrink-0 items-center justify-center rounded-full bg-slate-100 text-sm font-bold text-slate-600 group-hover:bg-teal-50 group-hover:text-teal-600">
              {i + 1}
            </div>
            <div className="flex flex-1 flex-col gap-1">
              <span className="text-[15px] font-semibold text-slate-900">
                {topic.title}
              </span>
              <span className="text-[13px] text-slate-500">
                {topic.description}
              </span>
            </div>
            <ChevronRight className="h-4 w-4 shrink-0 text-slate-400 transition-transform group-hover:translate-x-1" />
          </Link>
        ))}
      </div>
    </>
  );
}
```

- [ ] **Step 4: Typecheck**

Run: `cd dx-web && npx tsc --noEmit 2>&1 | tail -20`
Expected: zero output.

- [ ] **Step 5: Commit**

```bash
cd dx-web && git add src/features/web/docs/components/docs-home-hero.tsx src/features/web/docs/components/docs-category-grid.tsx src/features/web/docs/components/docs-category-index.tsx && git commit -m "feat(docs): add landing hero, category grid, and category index"
```

---

## Task 9: Create docs layout.tsx and delete placeholder

**Files:**
- Create: `src/app/(web)/(home)/docs/layout.tsx`
- Delete: `src/features/web/home/components/docs/docs-content.tsx`

- [ ] **Step 1: Create the docs root layout**

Create `src/app/(web)/(home)/docs/layout.tsx`:

```tsx
import type { ReactNode } from "react";
import { cookies } from "next/headers";
import { LandingHeader } from "@/components/in/landing-header";
import { Footer } from "@/components/in/footer";
import { DocsSidebar } from "@/features/web/docs/components/docs-sidebar";
import { DocsSidebarDrawer } from "@/features/web/docs/components/docs-sidebar-drawer";

export default async function DocsLayout({
  children,
}: {
  children: ReactNode;
}) {
  const cookieStore = await cookies();
  const isLoggedIn = !!cookieStore.get("dx_token")?.value;

  return (
    <div className="flex min-h-screen flex-col bg-white">
      <LandingHeader isLoggedIn={isLoggedIn} />
      <div className="h-px w-full bg-slate-200" />

      <div className="sticky top-0 z-10 flex h-12 items-center gap-2 border-b border-slate-200 bg-white px-4 lg:hidden">
        <DocsSidebarDrawer />
      </div>

      <div className="flex flex-1 bg-slate-50">
        <div className="mx-auto flex w-full max-w-[1280px]">
          <aside className="hidden w-[260px] shrink-0 border-r border-slate-200 bg-white px-5 py-6 lg:block">
            <DocsSidebar />
          </aside>
          <main className="flex flex-1 flex-col gap-8 px-4 py-6 md:px-8 lg:px-14 lg:py-10">
            {children}
          </main>
        </div>
      </div>

      <Footer />
    </div>
  );
}
```

- [ ] **Step 2: Delete the placeholder content component**

```bash
cd dx-web && rm src/features/web/home/components/docs/docs-content.tsx
# Also remove the empty directory if it becomes empty
rmdir src/features/web/home/components/docs 2>/dev/null || true
```

- [ ] **Step 3: Typecheck**

Run: `cd dx-web && npx tsc --noEmit 2>&1 | tail -20`

Expected: may fail because `src/app/(web)/(home)/docs/page.tsx` still imports `DocsPageContent`. That's fine — Task 10 rewrites `page.tsx`. Note any errors referencing `docs-content` and move to the next step.

- [ ] **Step 4: Commit**

```bash
cd dx-web && git add src/app/\(web\)/\(home\)/docs/layout.tsx src/features/web/home/components/docs/ && git commit -m "feat(docs): add root docs layout, remove placeholder docs-content"
```

---

## Task 10: Rewrite landing page.tsx and add dynamic routes + smoke test first topic

This task is the end-to-end smoke test: it finalizes the 3 route files, creates the first topic (1.1 认识斗学), registers it, and verifies the whole pipeline works before content-writing tasks begin.

**Files:**
- Modify: `src/app/(web)/(home)/docs/page.tsx` (rewrite — currently imports the deleted `DocsPageContent`)
- Create: `src/app/(web)/(home)/docs/[category]/page.tsx`
- Create: `src/app/(web)/(home)/docs/[category]/[topic]/page.tsx`
- Create: `src/features/web/docs/topics/getting-started/what-is-douxue.tsx`
- Modify: `src/features/web/docs/registry.ts` (import the first topic, register a single category with a single topic)

- [ ] **Step 1: Rewrite docs landing page**

Replace the entire contents of `src/app/(web)/(home)/docs/page.tsx` with:

```tsx
import type { Metadata } from "next";
import { DocsHomeHero } from "@/features/web/docs/components/docs-home-hero";
import { DocsCategoryGrid } from "@/features/web/docs/components/docs-category-grid";

export const metadata: Metadata = {
  title: "斗学帮助中心",
  description: "了解每一项功能的详细用法，让学习更顺畅。",
};

export default function DocsLandingPage() {
  return (
    <>
      <DocsHomeHero />
      <DocsCategoryGrid />
    </>
  );
}
```

- [ ] **Step 2: Create category index route**

Create `src/app/(web)/(home)/docs/[category]/page.tsx`:

```tsx
import type { Metadata } from "next";
import { notFound } from "next/navigation";
import { DocsCategoryIndex } from "@/features/web/docs/components/docs-category-index";
import { DOC_CATEGORIES, findCategory } from "@/features/web/docs/registry";

type Params = { category: string };

export function generateStaticParams(): Params[] {
  return DOC_CATEGORIES.map((c) => ({ category: c.slug }));
}

export async function generateMetadata({
  params,
}: {
  params: Promise<Params>;
}): Promise<Metadata> {
  const { category } = await params;
  const found = findCategory(category);
  if (!found) return { title: "未找到 — 斗学帮助中心" };
  return {
    title: `${found.title} — 斗学帮助中心`,
    description: found.description,
  };
}

export default async function CategoryPage({
  params,
}: {
  params: Promise<Params>;
}) {
  const { category } = await params;
  const found = findCategory(category);
  if (!found) notFound();
  return <DocsCategoryIndex category={found} />;
}
```

- [ ] **Step 3: Create topic route**

Create `src/app/(web)/(home)/docs/[category]/[topic]/page.tsx`:

```tsx
import type { Metadata } from "next";
import { notFound } from "next/navigation";
import { DocsBreadcrumb } from "@/features/web/docs/components/docs-breadcrumb";
import { DocsPrevNext } from "@/features/web/docs/components/docs-prev-next";
import { DocsToc } from "@/features/web/docs/components/docs-toc";
import { DOC_CATEGORIES, findTopic } from "@/features/web/docs/registry";

type Params = { category: string; topic: string };

export function generateStaticParams(): Params[] {
  return DOC_CATEGORIES.flatMap((c) =>
    c.topics.map((t) => ({ category: c.slug, topic: t.slug })),
  );
}

export async function generateMetadata({
  params,
}: {
  params: Promise<Params>;
}): Promise<Metadata> {
  const { category, topic } = await params;
  const found = findTopic(category, topic);
  if (!found) return { title: "未找到 — 斗学帮助中心" };
  return {
    title: `${found.ref.topic.title} — 斗学帮助中心`,
    description: found.ref.topic.description,
  };
}

export default async function TopicPage({
  params,
}: {
  params: Promise<Params>;
}) {
  const { category, topic } = await params;
  const found = findTopic(category, topic);
  if (!found) notFound();
  const { ref, prev, next } = found;
  const TopicComponent = ref.topic.Component;

  return (
    <div className="flex gap-8">
      <div className="flex flex-1 flex-col gap-8">
        <DocsBreadcrumb category={ref.category} topic={ref.topic} />
        <div className="flex flex-col gap-3">
          <h1 className="text-2xl font-extrabold tracking-tight text-slate-900 md:text-4xl">
            {ref.topic.title}
          </h1>
          <p className="text-base leading-relaxed text-slate-500">
            {ref.topic.description}
          </p>
        </div>
        <div className="h-px w-full bg-slate-200" />
        <TopicComponent />
        <div className="h-px w-full bg-slate-200" />
        <DocsPrevNext prev={prev} next={next} />
      </div>
      <DocsToc />
    </div>
  );
}
```

- [ ] **Step 4: Create the smoke topic (1.1 认识斗学)**

Create `src/features/web/docs/topics/getting-started/what-is-douxue.tsx`:

```tsx
import { BookOpen, Brain, Gamepad2, Users } from "lucide-react";
import { DocCallout } from "@/features/web/docs/primitives/doc-callout";
import { DocFeatureGrid } from "@/features/web/docs/primitives/doc-feature-grid";
import { DocSection } from "@/features/web/docs/primitives/doc-section";

export default function WhatIsDouxue() {
  return (
    <>
      <DocSection id="what" title="斗学是什么？">
        <p>
          斗学是一款融合游戏化机制与 AI 辅助的英语学习平台。它把背单词、做题、练听说这些传统学习动作，重新设计成有节奏、有反馈、可以和朋友一起玩的游戏流程，让学习这件事本身变得足够有趣，愿意每天坚持。
        </p>
        <p>
          和大多数刷题型 App 不同，斗学强调&ldquo;边玩边学&rdquo;：你不是在完成任务，而是在闯关、对战、协作，学习效果是副产品。
        </p>
      </DocSection>

      <DocSection id="core-values" title="我们相信什么">
        <p>
          我们相信&ldquo;以句子为单位学英语&rdquo;比孤立背单词更接近真实语言的使用方式。我们相信好的学习工具应当是&ldquo;陪你一起玩&rdquo;的，不是&ldquo;监督你打卡&rdquo;的。我们相信重复与反馈是掌握英语的真正核心，而这两件事都可以被游戏化做得更愉快。
        </p>
        <DocCallout variant="tip" title="小贴士">
          斗学支持网页端，在 Chrome、Safari、Edge 等主流浏览器中都能获得最佳体验。
        </DocCallout>
      </DocSection>

      <DocSection id="four-pillars" title="四大核心能力">
        <p>下面这四件事，构成了斗学日常使用的主要场景：</p>
        <DocFeatureGrid
          columns={2}
          items={[
            {
              icon: Gamepad2,
              iconColor: "text-teal-600",
              title: "三种学习模式",
              desc: "单人闯关、PK 对战、小组共学，同一套知识可以用三种不同节奏来学。",
            },
            {
              icon: BookOpen,
              iconColor: "text-blue-600",
              title: "词汇追踪系统",
              desc: "生词本、复习本、已掌握三本书自动联动，间隔重复帮你把词真正记牢。",
            },
            {
              icon: Users,
              iconColor: "text-amber-600",
              title: "社区与小组",
              desc: "在斗学社发帖交流，加入学习小组和朋友一起组局学习。",
            },
            {
              icon: Brain,
              iconColor: "text-purple-600",
              title: "AI 智能创作",
              desc: "给定关键词，让 AI 帮你生成属于你自己的课程内容和词汇关卡。",
            },
          ]}
        />
      </DocSection>

      <DocSection id="who" title="适合谁用">
        <p>
          斗学适合所有想把英语学扎实、但又受不了&ldquo;坐下来背书&rdquo;的人：想提升词汇量的学生、想捡起英语的上班族、希望孩子有趣地学英语的家长、或者单纯觉得刷题太无聊的你。
        </p>
      </DocSection>
    </>
  );
}
```

- [ ] **Step 5: Register the first category and topic in the registry**

Edit `src/features/web/docs/registry.ts` — replace the top of the file (imports and `DOC_CATEGORIES` array) with:

```ts
import { BookOpen } from "lucide-react";
import type { DocCategory, TopicRef } from "./types";
import WhatIsDouxue from "./topics/getting-started/what-is-douxue";

export const DOC_CATEGORIES: DocCategory[] = [
  {
    slug: "getting-started",
    title: "开始使用",
    description: "第一次使用斗学？从这里开始。",
    icon: BookOpen,
    accentClass: "border-teal-200 bg-teal-50 text-teal-600",
    topics: [
      {
        slug: "what-is-douxue",
        title: "认识斗学",
        description:
          "斗学是一款融合游戏化机制与 AI 辅助的英语学习平台。来了解它的核心理念和四大能力。",
        Component: WhatIsDouxue,
      },
    ],
  },
];
```

Leave the helper functions (`findCategory`, `flatTopics`, `findTopic`) unchanged.

- [ ] **Step 6: Typecheck**

Run: `cd dx-web && npx tsc --noEmit 2>&1 | tail -20`
Expected: zero output.

- [ ] **Step 7: Run full build as smoke test**

Run: `cd dx-web && npm run build 2>&1 | tail -30`

Expected output must include lines like:

```
○  (Static)  /docs
●  (SSG)     /docs/[category]
●  (SSG)     /docs/[category]/[topic]
```

And a successful build summary (`✓ Compiled successfully`). If any error appears, stop and diagnose before moving on.

- [ ] **Step 8: Manual route verification**

Start dev server: `cd dx-web && npm run dev`

Visit in a browser:
- `http://localhost:3000/docs` — should show hero + single category card "开始使用"
- `http://localhost:3000/docs/getting-started` — should show category page with one topic row "认识斗学"
- `http://localhost:3000/docs/getting-started/what-is-douxue` — should show full topic page with breadcrumb, H1, lead, four H2 sections, and prev/next footer (both empty since there's only one topic)
- `http://localhost:3000/docs/nonexistent` — should 404
- `http://localhost:3000/docs/getting-started/nonexistent` — should 404

Also verify:
- Sidebar on the left shows "开始使用" category with the topic highlighted when viewing the topic page
- Resize browser to <1024px width: sidebar hides, a "目录" button appears in the top strip
- Click the drawer button: sidebar opens as a sheet from the left

If all checks pass, stop the dev server.

- [ ] **Step 9: Commit**

```bash
cd dx-web && git add src/app/\(web\)/\(home\)/docs/page.tsx src/app/\(web\)/\(home\)/docs/\[category\]/ src/features/web/docs/registry.ts src/features/web/docs/topics/getting-started/ && git commit -m "feat(docs): wire /docs routes end-to-end with first smoke topic"
```

---

## Content task template (applies to Tasks 11–22)

Every content task follows this shape. Read this once before executing Tasks 11–22.

**Per-task pattern:**

1. Read the referenced spec section in `docs/superpowers/specs/2026-04-13-docs-page-design.md` for the bullet outline of each topic.
2. Create one TSX file per topic under `src/features/web/docs/topics/<category>/<topic-slug>.tsx`.
3. Each topic file is a zero-arg default-export server component that renders content via the primitives from Tasks 2–5.
4. Each topic renders 3–5 `<DocSection>` blocks, each with an explicit `id` matching a stable English anchor.
5. Prose is written in Simplified Chinese, casual second-person tone (你 not 您), concise and direct. 2–4 sentences per prose paragraph. Match the tone established in Task 10's `what-is-douxue.tsx`.
6. Primitives used per topic are listed in each task — always prefer the specified primitive over ad-hoc JSX.
7. After creating topic files, edit `registry.ts` to append a new category entry (or extend an existing one) with all topics and imports.
8. Each topic's `description` field in the registry is 1–2 sentences that become the topic page lead paragraph.
9. Run `npx tsc --noEmit` after edits to catch errors.
10. Commit the whole task as one commit.

**Topic file scaffold (copy as starting point for every new topic):**

```tsx
import { DocSection } from "@/features/web/docs/primitives/doc-section";
// Add other primitive imports as needed, e.g.:
// import { DocCallout } from "@/features/web/docs/primitives/doc-callout";
// import { DocSteps } from "@/features/web/docs/primitives/doc-steps";

export default function TopicComponent() {
  return (
    <>
      <DocSection id="section-anchor-1" title="节标题 1">
        <p>2–4 句中文正文。</p>
      </DocSection>
      {/* more sections */}
    </>
  );
}
```

**Registry append pattern (inside `DOC_CATEGORIES`):**

```ts
{
  slug: "category-slug",
  title: "中文分类名",
  description: "一句话描述",
  icon: LucideIconName,
  accentClass: "border-{color}-200 bg-{color}-50 text-{color}-600",
  topics: [
    {
      slug: "topic-slug",
      title: "中文主题名",
      description: "一到两句话，作为主题页 lead paragraph",
      Component: TopicComponent,
    },
    // more topics
  ],
},
```

**Primitive import paths** (always use direct paths, not barrel):
- `@/features/web/docs/primitives/doc-section`
- `@/features/web/docs/primitives/doc-callout`
- `@/features/web/docs/primitives/doc-steps`
- `@/features/web/docs/primitives/doc-feature-grid`
- `@/features/web/docs/primitives/doc-compare-table`
- `@/features/web/docs/primitives/doc-key-value`
- `@/features/web/docs/primitives/doc-flow`
- `@/features/web/docs/primitives/doc-slug`
- `@/features/web/docs/primitives/doc-link`
- `@/features/web/docs/primitives/doc-pseudo-ui`
- `@/features/web/docs/primitives/doc-code-block`
- `@/features/web/docs/primitives/doc-faq-accordion`

**Prose guidance:**
- Tone: casual, direct, 你 not 您. Match Task 10 exemplar.
- Length: each prose paragraph is 2–4 sentences. Each DocSection has 1–3 prose paragraphs plus 0–1 primitive blocks.
- Never copy marketing language from other products or from the sample menu the user supplied — stick to what the codebase actually does.
- Verify every numeric claim, rule, or limit against either the spec file or the current code before writing.
- Never invent features that don't exist in the codebase.

---

## Task 11: Content — Getting Started (3 remaining topics: 1.2, 1.3, 1.4)

Topic 1.1 was already created in Task 10. This task adds the remaining three topics in the "开始使用" category.

**Spec reference:** `docs/superpowers/specs/2026-04-13-docs-page-design.md` lines 49–54 — bullet outlines for topics 1.2, 1.3, 1.4.

**Files:**
- Create: `src/features/web/docs/topics/getting-started/signup-signin.tsx`
- Create: `src/features/web/docs/topics/getting-started/hall-tour.tsx`
- Create: `src/features/web/docs/topics/getting-started/first-session.tsx`
- Modify: `src/features/web/docs/registry.ts` (append 3 topics to getting-started category)

- [ ] **Step 1: Create 1.2 注册与登录**

File: `src/features/web/docs/topics/getting-started/signup-signin.tsx`

Structure:
- Import `DocSection`, `DocSteps`, `DocCallout`, `DocKeyValue`
- Section 1 `id="three-ways"` "三种注册方式" — describe the three sign-up/sign-in paths in prose + `DocFeatureGrid` (3 cards: 邮箱验证码 / 账号密码 / 微信扫码)
- Section 2 `id="rules"` "账号规则" — `DocKeyValue` with items:
  - `{ key: "用户名", value: "最多 30 字符，仅允许字母、数字、下划线 `_`、连字符 `-`" }`
  - `{ key: "密码", value: "至少 8 字符，需包含大写、小写字母和数字" }`
  - `{ key: "验证码", value: "6 位数字，发送后可请求倒计时" }`
- Section 3 `id="forgot"` "忘记密码" — 2 sentences + `DocSteps`:
  - `{ title: "前往登录页", desc: "点击&ldquo;忘记密码&rdquo;链接" }`
  - `{ title: "输入邮箱", desc: "接收 6 位数字验证码" }`
  - `{ title: "设置新密码", desc: "按密码规则重设" }`
- Section 4 `id="invited"` "被朋友邀请注册" — explain that 如果是通过朋友的邀请链接来到斗学，完成注册后这个邀请关系会自动建立，朋友会在他的推广页看到你。Add a `<DocCallout variant="tip">` telling them they don't need to input anything extra — the system handles it via cookie.

**Description for registry:** "邮箱验证码、账号密码、微信扫码三种方式都可以，加上完整的账号规则和忘记密码流程。"

- [ ] **Step 2: Create 1.3 学习首页导览**

File: `src/features/web/docs/topics/getting-started/hall-tour.tsx`

Structure:
- Import `DocSection`, `DocFeatureGrid`, `DocKeyValue`, `DocCallout`
- Section 1 `id="overview"` "学习大厅长什么样" — describe the 8 dashboard blocks (问候语条、通知横幅、每日挑战、游戏进度、今日之星、数据行、学习热力图、广告位) in prose. 2 paragraphs.
- Section 2 `id="sidebar"` "侧边栏导航" — explain the sidebar has 12 categories mapped to different features. 1 paragraph. Recommend 把鼠标悬停在图标上可以看到每项的完整名称.
- Section 3 `id="stats-row"` "数据行代表什么" — `DocKeyValue`:
  - `{ key: "经验值 (EXP)", value: "完成闯关和每日挑战累积" }`
  - `{ key: "连续打卡", value: "当前连续学习天数，与最高纪录" }`
  - `{ key: "掌握单词", value: "本周新增 / 总量" }`
  - `{ key: "待复习", value: "需要今天复习的条目数量" }`
- Section 4 `id="daily-challenge"` "每日挑战" — explain the two fixed daily tasks: (1) 完成任意连词成句闯关可获得双倍经验；(2) 在斗学社发一篇帖子。Add a `DocCallout variant="info"` reminding that 每日挑战每天重置一次.

**Description for registry:** "登录进入大厅后，你会看到仪表盘、侧边栏、数据行、每日挑战。这里是每一块区域的含义。"

- [ ] **Step 3: Create 1.4 新手第一课**

File: `src/features/web/docs/topics/getting-started/first-session.tsx`

Structure:
- Import `DocSection`, `DocSteps`, `DocCallout`, `DocLink`
- Section 1 `id="why"` "为什么从这里开始" — 1 paragraph explaining this is the shortest path to the core Douxue experience, about 10 minutes.
- Section 2 `id="steps"` "10 分钟跑通学习流程" — `DocSteps` with 5 steps:
  - `{ title: "打开游戏页", desc: "在侧边栏点&ldquo;游戏&rdquo;，按分类或出版社挑一个感兴趣的课程" }`
  - `{ title: "进入游戏详情", desc: "点击游戏卡片，查看关卡列表和游戏简介" }`
  - `{ title: "选第一关", desc: "免费用户只能玩每个游戏的第一关，直接点第一关即可" }`
  - `{ title: "选择难度和学习模式", desc: "推荐初级 + 写，门槛最低，熟悉玩法后再尝试其它组合" }`
  - `{ title: "开始闯关", desc: "按提示完成答题，正确率达到 60% 即可获得经验" }`
- Section 3 `id="after"` "结算页能看到什么" — describe: 得分、评级、获得的经验、错题回顾、下一关按钮。
- Section 4 `id="what-next"` "接下来可以做什么" — suggest: 看一下 <DocLink href="/docs/learning-modes/overview">三种学习模式</DocLink>，或直接尝试 PK 模式挑战一下机器人（需要会员）。1 short paragraph.

**Description for registry:** "10 分钟跑通第一次学习：挑游戏、选关卡、选难度、完成答题、看结算。"

- [ ] **Step 4: Update registry**

Edit `src/features/web/docs/registry.ts`:

1. Add imports at the top:
```ts
import SignupSignin from "./topics/getting-started/signup-signin";
import HallTour from "./topics/getting-started/hall-tour";
import FirstSession from "./topics/getting-started/first-session";
```

2. Extend the `getting-started` category's `topics` array to include all 4:
```ts
topics: [
  {
    slug: "what-is-douxue",
    title: "认识斗学",
    description:
      "斗学是一款融合游戏化机制与 AI 辅助的英语学习平台。来了解它的核心理念和四大能力。",
    Component: WhatIsDouxue,
  },
  {
    slug: "signup-signin",
    title: "注册与登录",
    description:
      "邮箱验证码、账号密码、微信扫码三种方式都可以，加上完整的账号规则和忘记密码流程。",
    Component: SignupSignin,
  },
  {
    slug: "hall-tour",
    title: "学习首页导览",
    description:
      "登录进入大厅后，你会看到仪表盘、侧边栏、数据行、每日挑战。这里是每一块区域的含义。",
    Component: HallTour,
  },
  {
    slug: "first-session",
    title: "新手第一课",
    description: "10 分钟跑通第一次学习：挑游戏、选关卡、选难度、完成答题、看结算。",
    Component: FirstSession,
  },
],
```

- [ ] **Step 5: Typecheck**

Run: `cd dx-web && npx tsc --noEmit 2>&1 | tail -20`
Expected: zero output.

- [ ] **Step 6: Commit**

```bash
cd dx-web && git add src/features/web/docs/topics/getting-started/ src/features/web/docs/registry.ts && git commit -m "feat(docs): write 3 remaining getting-started topics"
```

---

## Task 12: Content — 三种学习模式 (5 topics)

This is the emphasized category. Topic 2.3 (PK 对战模式) is the primitive-heaviest page and should serve as a quality exemplar.

**Spec reference:** lines 56–62 of the spec file for topic outlines.

**Files:**
- Create: `src/features/web/docs/topics/learning-modes/overview.tsx`
- Create: `src/features/web/docs/topics/learning-modes/single-mode.tsx`
- Create: `src/features/web/docs/topics/learning-modes/pk-mode.tsx`
- Create: `src/features/web/docs/topics/learning-modes/group-mode.tsx`
- Create: `src/features/web/docs/topics/learning-modes/game-types.tsx`
- Modify: `src/features/web/docs/registry.ts` (append new category)

- [ ] **Step 1: Create 2.1 模式总览**

File: `src/features/web/docs/topics/learning-modes/overview.tsx`

Structure:
- Import `DocSection`, `DocCompareTable`, `DocCallout`
- Section `id="three-modes"` "三种模式" — 2 paragraphs introducing single / PK / group and their general character.
- Section `id="compare"` "一表看懂" — `DocCompareTable` with:
  - `columns={["单人闯关", "PK 对战", "小组共学"]}`
  - `labelHeader="维度"`
  - rows: `参与人数` → `["1 人", "2 人", "多人"]`; `对手` → `["无（自己）", "机器人 / 真人", "小组成员"]`; `VIP 要求` → `[false, true, true]` (single free, PK VIP only, group requires VIP to create a group); `是否实时` → `[false, true, true]`; `典型场景` → `["自由练习", "紧张刺激", "组队学习"]`
- Section `id="shared-basics"` "三种模式的共同基础" — 1 paragraph explaining all three modes share the same game type / difficulty / pattern system, and link to 2.5 via `DocLink`.
- Section `id="how-to-choose"` "我该选哪个" — `DocCallout variant="tip"` listing 3 recommendations (独自练习 → 单人; 和机器人热身 → PK; 和朋友一起组局 → 小组).

**Description:** "三种模式对比一张表看懂：人数、对手、VIP 要求、典型场景。"

- [ ] **Step 2: Create 2.2 单人闯关模式**

File: `src/features/web/docs/topics/learning-modes/single-mode.tsx`

Structure:
- Import `DocSection`, `DocSteps`, `DocCallout`, `DocFlow`, `DocSlug`
- Section `id="what"` "什么是单人闯关" — 2 paragraphs: this is the default mode, everyone can use it, based on picking a game and playing its levels one by one.
- Section `id="start"` "如何开始一局" — `DocSteps`:
  - `{ title: "挑游戏", desc: "从游戏广场里挑一个感兴趣的课程包" }`
  - `{ title: "进入详情页", desc: "选择一个关卡" }`
  - `{ title: "选 degree / pattern / difficulty", desc: "初/中/高级 × 听/说/读/写 × 简单/普通/困难" }`
  - `{ title: "开始答题", desc: "按游戏类型答题，关卡结束看结算" }`
- Section `id="levels"` "关卡解锁规则" — explain: 免费用户只能玩每个游戏的第一关; VIP 可解锁全部关卡. Add `DocCallout variant="warning"` flagging the VIP gate.
- Section `id="session-lifecycle"` "会话机制" — use `DocFlow` with nodes: `[{ label: "start", desc: "开始会话" }, { label: "answer / skip", desc: "答题或跳过" }, { label: "complete", desc: "关卡完成" }]`. 1 paragraph explaining that if you exit mid-session, the next time you enter the same level, your progress is restored.
- Section `id="combo"` "连击与评分" — 1 paragraph summary, then `DocLink` to `/docs/progress/combo-rating`.

**Description:** "默认模式，谁都能玩。介绍如何开始、关卡解锁规则、会话机制和连击奖励。"

- [ ] **Step 3: Create 2.3 PK 对战模式 (exemplar for primitive-heavy pages)**

File: `src/features/web/docs/topics/learning-modes/pk-mode.tsx`

Structure:
- Import `DocSection`, `DocCallout`, `DocCompareTable`, `DocKeyValue`, `DocSteps`, `DocFlow`
- Section `id="what"` "什么是 PK 模式" — 2 paragraphs: real-time 1v1, whoever finishes the level first wins. `DocCallout variant="warning" title="仅 VIP 可用"` clarifying the VIP gate.
- Section `id="two-types"` "两种对战方式" — `DocCompareTable`:
  - `columns={["random（随机机器人）", "specified（指定真人）"]}`
  - `labelHeader="维度"`
  - rows: `对手` → `["AI 机器人", "在线的好友或陌生人"]`; `等待时间` → `["即开即战", "需要对方同意"]`; `对方资格` → `["—", "对方必须是 VIP 且在线"]`; `典型场景` → `["一个人想练节奏", "和朋友约战"]`
- Section `id="robot-difficulty"` "机器人三档难度" — intro sentence + `DocKeyValue`:
  - `{ key: "easy", value: "正确率 50–70%", note: "延迟 7–12 秒，连击打断率 50%" }`
  - `{ key: "normal", value: "正确率 70–85%", note: "延迟 5–8 秒，连击打断率 30%" }`
  - `{ key: "hard", value: "正确率 85–95%", note: "延迟 3–5 秒，连击打断率 10%" }`
- Section `id="invite-flow"` "邀请真人对战" — `DocSteps`:
  - `{ title: "选游戏 / 关卡 / 模式", desc: "和单人模式一样，先把本次对战的游戏和参数定好" }`
  - `{ title: "选择对手", desc: "从在线好友列表里挑一个对手" }`
  - `{ title: "发起邀请", desc: "系统会实时通知对方" }`
  - `{ title: "等待回应", desc: "对方可以接受或拒绝，接受后双方自动进入对战房间" }`
- Section `id="invite-lifecycle"` "邀请状态流转" — `DocFlow` with nodes: `[{ label: "pending", desc: "等待回应" }, { label: "accepted", desc: "对战中" }, { label: "declined", desc: "已拒绝" }]`. 1 paragraph noting 如果对方长时间未回应，邀请可能会过期.
- Section `id="results"` "对战结算" — 2 paragraphs: first finisher wins, both players get EXP, ranking shown at end.

**Description:** "与真人或 AI 机器人实时对战，抢先完成关卡获胜。PK 模式需要 VIP。"

- [ ] **Step 4: Create 2.4 小组共学模式**

File: `src/features/web/docs/topics/learning-modes/group-mode.tsx`

Structure:
- Import `DocSection`, `DocCallout`, `DocCompareTable`, `DocLink`, `DocSteps`
- Section `id="what"` "什么是小组共学" — explain: 你需要先加入一个学习小组; 组主开局后，所有组员进入同一个房间开始学习. 2 paragraphs.
- Section `id="two-formats"` "两种开局方式" — `DocCompareTable`:
  - `columns={["group_solo（个人排名）", "group_team（分组对战）"]}`
  - `labelHeader="维度"`
  - rows: `参与方式` → `["组内所有人各自闯同一游戏", "分为多个子分组互相对抗"]`; `结算依据` → `["个人分数排名", "子分组汇总分数"]`; `适合人数` → `["任意", "人多时更有意思"]`
- Section `id="how-to-start"` "组主如何开局" — `DocSteps`:
  - `{ title: "选游戏", desc: "组主在小组页选择本次学习要玩的游戏" }`
  - `{ title: "选模式", desc: "group_solo 或 group_team" }`
  - `{ title: "设置起始关卡", desc: "所有人从这关开始" }`
  - `{ title: "开始游戏", desc: "全员进入房间" }`
- Section `id="progress"` "关卡推进" — 1 paragraph: 组主可以 `next-level` 推进到下一关，或 `force-end` 提前结束. Link to 小组管理 via `DocLink`.
- Section `id="need-group"` "我还没有小组" — `DocCallout variant="info"` directing users to 7.2 创建与加入.

**Description:** "加入一个学习小组，组主开局后所有组员一起闯关。支持个人排名和分组对战两种格式。"

- [ ] **Step 5: Create 2.5 游戏类型与技能矩阵**

File: `src/features/web/docs/topics/learning-modes/game-types.tsx`

Structure:
- Import `DocSection`, `DocFeatureGrid`, `DocCompareTable`, `DocKeyValue`, `DocSlug`
- Import Lucide icons for the 4 game types (pick: `BookOpen`, `Swords`, `Layers`, `Sparkles` — or similar)
- Section `id="four-games"` "四种游戏类型" — `DocFeatureGrid columns={2}` with four cards:
  - `{ title: "连词成句 (word-sentence)", desc: "把打乱顺序的单词拼成正确句子" }`
  - `{ title: "词汇对轰 (vocab-battle)", desc: "快速识别单词中英文对应关系" }`
  - `{ title: "词汇配对 (vocab-match)", desc: "成对连线匹配单词与释义" }`
  - `{ title: "词汇消消乐 (vocab-elimination)", desc: "消除匹配的单词对，越玩越快" }`
- Section `id="difficulty"` "三个难度等级" — `DocKeyValue`:
  - `{ key: "beginner（初级）", value: "所有内容类型可选", note: "适合新手" }`
  - `{ key: "intermediate（中级）", value: "仅组合、短语、句子" }`
  - `{ key: "advanced（高级）", value: "仅句子" }`
- Section `id="patterns"` "四种学习模式（听说读写）" — 1 paragraph + `DocFeatureGrid columns={4}`:
  - 4 cards: listen / speak / read / write with Lucide icons (Headphones, Mic, Eye, PenLine) and short descriptions
- Section `id="matrix"` "难度 × 学习模式矩阵" — 1 paragraph: 每种难度都可以和任意一种学习模式组合，例如 beginner + listen 或 advanced + write. `DocCompareTable` with columns `["listen", "speak", "read", "write"]` and rows `beginner/intermediate/advanced` all showing `true`.

**Description:** "四种游戏类型 × 三种难度 × 四种学习模式，让同一套知识可以用几十种组合来练。"

- [ ] **Step 6: Update registry — add new category**

Edit `src/features/web/docs/registry.ts`:

1. Add imports:
```ts
import { Swords } from "lucide-react";
import LearningModesOverview from "./topics/learning-modes/overview";
import SingleMode from "./topics/learning-modes/single-mode";
import PkMode from "./topics/learning-modes/pk-mode";
import GroupMode from "./topics/learning-modes/group-mode";
import GameTypes from "./topics/learning-modes/game-types";
```

2. Append to `DOC_CATEGORIES` array (after the first `getting-started` category):
```ts
{
  slug: "learning-modes",
  title: "三种学习模式",
  description: "单人、PK、小组三种玩法的完整介绍，斗学的核心玩法都在这里。",
  icon: Swords,
  accentClass: "border-rose-200 bg-rose-50 text-rose-600",
  topics: [
    {
      slug: "overview",
      title: "模式总览",
      description: "三种模式的对比表：参与人数、对手、VIP 要求、典型场景。",
      Component: LearningModesOverview,
    },
    {
      slug: "single-mode",
      title: "单人闯关模式",
      description: "默认模式，所有人可玩。了解如何开始、关卡解锁规则、会话机制。",
      Component: SingleMode,
    },
    {
      slug: "pk-mode",
      title: "PK 对战模式",
      description: "与真人或 AI 机器人实时对战，抢先完成关卡获胜。需要 VIP。",
      Component: PkMode,
    },
    {
      slug: "group-mode",
      title: "小组共学模式",
      description: "加入学习小组后一起闯关，支持个人排名和分组对战。",
      Component: GroupMode,
    },
    {
      slug: "game-types",
      title: "游戏类型与技能矩阵",
      description: "四种游戏类型 × 三个难度 × 四种学习模式，练习组合丰富。",
      Component: GameTypes,
    },
  ],
},
```

- [ ] **Step 7: Typecheck**

Run: `cd dx-web && npx tsc --noEmit 2>&1 | tail -20`
Expected: zero output.

- [ ] **Step 8: Commit**

```bash
cd dx-web && git add src/features/web/docs/topics/learning-modes/ src/features/web/docs/registry.ts && git commit -m "feat(docs): write learning-modes category (single/PK/group emphasis)"
```

---

## Task 13: Content — 课程与游戏 (3 topics)

**Spec reference:** lines 64–68.

**Files:**
- Create: `src/features/web/docs/topics/courses-games/browsing.tsx`
- Create: `src/features/web/docs/topics/courses-games/detail-levels.tsx`
- Create: `src/features/web/docs/topics/courses-games/favorites.tsx`
- Modify: `src/features/web/docs/registry.ts`

- [ ] **Step 1: Create 3.1 挑选游戏**

File: `src/features/web/docs/topics/courses-games/browsing.tsx`

Structure:
- Import `DocSection`, `DocFeatureGrid`, `DocCallout`
- Section `id="layout"` "游戏广场长什么样" — describe: 顶部是筛选栏，下方是卡片网格，支持无限滚动. 1 paragraph.
- Section `id="filters"` "三个筛选维度" — `DocFeatureGrid columns={3}` with:
  - `{ title: "按分类", desc: "树形分类浏览（如 K12 / 雅思 / 日常）" }`
  - `{ title: "按出版社", desc: "按教材或内容方筛选" }`
  - `{ title: "按游戏模式", desc: "连词成句 / 词汇对轰 / 词汇配对 / 词汇消消乐" }`
- Section `id="search"` "按名称搜索" — 1 paragraph: 在筛选栏附近有搜索框，支持按游戏名称关键字搜索.
- Section `id="cards"` "游戏卡片上的信息" — 1 paragraph listing what the card shows: 封面图、名称、作者、所属分类、游戏模式. Close with a `DocCallout variant="tip"` reminding that 收藏的游戏会在&ldquo;我的收藏&rdquo;里单独列出.

**Description:** "游戏广场的筛选、搜索和卡片结构一览。"

- [ ] **Step 2: Create 3.2 游戏详情与关卡**

File: `src/features/web/docs/topics/courses-games/detail-levels.tsx`

Structure:
- Import `DocSection`, `DocCallout`, `DocLink`
- Section `id="detail"` "游戏详情页" — 1 paragraph: 封面、简介、作者、模式、关卡列表.
- Section `id="level-grid"` "关卡网格" — 1 paragraph: 所有关卡按顺序排列，每一关都可以独立进入.
- Section `id="first-level-free"` "首关免费规则" — `DocCallout variant="warning"` stating 每个游戏的第一关对所有用户开放；其余关卡需要 VIP 会员. 1 paragraph before the callout explaining why this design exists (让你先试后买).
- Section `id="three-modes-from-here"` "从这里进入三种模式" — 1 paragraph: 点击任意关卡后，选择单人、PK 或小组模式开始游戏. Link to `/docs/learning-modes/overview` via `DocLink`.

**Description:** "游戏详情页、关卡网格、首关免费规则，以及如何从这里进入三种学习模式。"

- [ ] **Step 3: Create 3.3 收藏与我的游戏**

File: `src/features/web/docs/topics/courses-games/favorites.tsx`

Structure:
- Import `DocSection`
- Section `id="favorite"` "如何收藏" — 1 paragraph: 在游戏卡片或详情页点击&ldquo;收藏&rdquo;按钮即可加入收藏；再次点击取消收藏.
- Section `id="favorites-page"` "收藏列表" — 1 paragraph: 侧边栏的&ldquo;收藏&rdquo;页展示所有已收藏的游戏.
- Section `id="my-games"` "我的游戏" — 1 paragraph: &ldquo;我的游戏&rdquo;包含两类：你玩过的 和 你创建的. 前者按最近学习时间排序.
- Section `id="no-limit"` "没有数量限制" — 1 short sentence noting 收藏数量没有硬性上限.

**Description:** "收藏游戏、查看收藏列表、区分&ldquo;玩过的&rdquo;和&ldquo;我创建的&rdquo;游戏。"

- [ ] **Step 4: Update registry — add new category**

Edit `src/features/web/docs/registry.ts`:

1. Add imports:
```ts
import { Library } from "lucide-react";
import Browsing from "./topics/courses-games/browsing";
import DetailLevels from "./topics/courses-games/detail-levels";
import Favorites from "./topics/courses-games/favorites";
```

2. Append a new category entry to `DOC_CATEGORIES`:
```ts
{
  slug: "courses-games",
  title: "课程与游戏",
  description: "如何挑游戏、查看关卡、管理收藏。",
  icon: Library,
  accentClass: "border-blue-200 bg-blue-50 text-blue-600",
  topics: [
    {
      slug: "browsing",
      title: "挑选游戏",
      description: "游戏广场的筛选、搜索和卡片结构一览。",
      Component: Browsing,
    },
    {
      slug: "detail-levels",
      title: "游戏详情与关卡",
      description: "游戏详情页、关卡网格、首关免费规则。",
      Component: DetailLevels,
    },
    {
      slug: "favorites",
      title: "收藏与我的游戏",
      description: "收藏游戏、查看收藏列表、区分&ldquo;玩过&rdquo;和&ldquo;我创建&rdquo;。",
      Component: Favorites,
    },
  ],
},
```

- [ ] **Step 5: Typecheck**

Run: `cd dx-web && npx tsc --noEmit 2>&1 | tail -20`
Expected: zero output.

- [ ] **Step 6: Commit**

```bash
cd dx-web && git add src/features/web/docs/topics/courses-games/ src/features/web/docs/registry.ts && git commit -m "feat(docs): write courses-games category"
```

---

## Task 14: Content — 词汇管理 (3 topics)

**Spec reference:** lines 70–74.

**Files:**
- Create: `src/features/web/docs/topics/vocabulary/unknown.tsx`
- Create: `src/features/web/docs/topics/vocabulary/review.tsx`
- Create: `src/features/web/docs/topics/vocabulary/mastered.tsx`
- Modify: `src/features/web/docs/registry.ts`

- [ ] **Step 1: Create 4.1 生词本**

File: `src/features/web/docs/topics/vocabulary/unknown.tsx`

Structure:
- Import `DocSection`, `DocCallout`, `DocKeyValue`, `DocLink`
- Section `id="add"` "如何加入生词" — 1 paragraph: 在闯关过程中遇到不认识的内容，点击&ldquo;加入生词本&rdquo;即可收入生词本.
- Section `id="stats"` "生词本页面能看到什么" — `DocKeyValue`:
  - `{ key: "生词总量", value: "到目前为止收藏的全部生词" }`
  - `{ key: "今日新增", value: "今天新加入的生词数量" }`
  - `{ key: "近 3 天", value: "最近三天积累的生词" }`
- Section `id="manage"` "单个和批量删除" — 1 paragraph explaining how to delete entries.
- Section `id="next"` "接下来呢" — 1 paragraph: 把生词本的词转到 <DocLink href="/docs/vocabulary/review">复习本</DocLink> 才会真正进入间隔重复.

**Description:** "收集你不认识的词，统计和管理生词，然后把它们转到复习本开始间隔重复。"

- [ ] **Step 2: Create 4.2 复习本与间隔重复**

File: `src/features/web/docs/topics/vocabulary/review.tsx`

Structure:
- Import `DocSection`, `DocKeyValue`, `DocCallout`, `DocLink`
- Section `id="spaced-repetition"` "什么是间隔重复" — 2 paragraphs explaining the general concept (based on memory science; gradually stretch the time between reviews).
- Section `id="intervals"` "斗学的间隔表" — `DocKeyValue`:
  - `{ key: "第 0 次", value: "1 天后再复习" }`
  - `{ key: "第 1 次", value: "3 天后" }`
  - `{ key: "第 2 次", value: "7 天后" }`
  - `{ key: "第 3 次", value: "14 天后" }`
  - `{ key: "第 4 次", value: "30 天后" }`
  - `{ key: "第 5 次起", value: "90 天后（最长间隔）" }`
- Section `id="three-states"` "三种状态" — 1 paragraph: 待复习 / 逾期 / 今日已复习. Describe what each means.
- Section `id="flow"` "复习后发生什么" — 1 paragraph: 完成一次复习后，系统按当前间隔推到下一档；如果连续答对 N 次，这条词可以转到已掌握. Link to `/docs/vocabulary/mastered`.

**Description:** "复习本按间隔重复节奏安排你的词汇复习。[1, 3, 7, 14, 30, 90] 天是斗学的节奏。"

- [ ] **Step 3: Create 4.3 已掌握**

File: `src/features/web/docs/topics/vocabulary/mastered.tsx`

Structure:
- Import `DocSection`, `DocKeyValue`
- Section `id="how"` "如何标为掌握" — 1 paragraph: 连续复习正确几次，或者手动在复习本中标记.
- Section `id="page"` "已掌握页面" — `DocKeyValue`:
  - `{ key: "累计掌握", value: "到目前为止掌握的总词数" }`
  - `{ key: "本周", value: "本周新掌握" }`
  - `{ key: "本月", value: "本月新掌握" }`
- Section `id="removed-from-review"` "不再出现在复习中" — 1 paragraph: 已掌握的词会自动从复习本移出，但不会从生词本物理删除.
- Section `id="undo"` "取消掌握" — 1 paragraph: 如果觉得自己其实没记牢，可以在&ldquo;已掌握&rdquo;页把这条词撤回，它会重新进入复习本.

**Description:** "把记牢的词标记为已掌握，不再出现在复习中；统计你的掌握进度。"

- [ ] **Step 4: Update registry**

Edit `src/features/web/docs/registry.ts`:

1. Add imports:
```ts
import { BookMarked } from "lucide-react";
import Unknown from "./topics/vocabulary/unknown";
import Review from "./topics/vocabulary/review";
import Mastered from "./topics/vocabulary/mastered";
```

2. Append new category:
```ts
{
  slug: "vocabulary",
  title: "词汇管理",
  description: "生词本、复习本、已掌握三本书自动联动，让词汇真正留下来。",
  icon: BookMarked,
  accentClass: "border-emerald-200 bg-emerald-50 text-emerald-600",
  topics: [
    {
      slug: "unknown",
      title: "生词本",
      description: "收集生词、查看统计、转入复习本。",
      Component: Unknown,
    },
    {
      slug: "review",
      title: "复习本与间隔重复",
      description: "按 [1, 3, 7, 14, 30, 90] 天节奏做间隔重复复习。",
      Component: Review,
    },
    {
      slug: "mastered",
      title: "已掌握",
      description: "标记已掌握的词，查看统计，可反向取消。",
      Component: Mastered,
    },
  ],
},
```

- [ ] **Step 5: Typecheck**

Run: `cd dx-web && npx tsc --noEmit 2>&1 | tail -20`
Expected: zero output.

- [ ] **Step 6: Commit**

```bash
cd dx-web && git add src/features/web/docs/topics/vocabulary/ src/features/web/docs/registry.ts && git commit -m "feat(docs): write vocabulary category (unknown/review/mastered)"
```

---

## Task 15: Content — 成长与激励 (4 topics)

**Spec reference:** lines 76–81.

**Files:**
- Create: `src/features/web/docs/topics/progress/exp-levels.tsx`
- Create: `src/features/web/docs/topics/progress/combo-rating.tsx`
- Create: `src/features/web/docs/topics/progress/play-streak.tsx`
- Create: `src/features/web/docs/topics/progress/leaderboard.tsx`
- Modify: `src/features/web/docs/registry.ts`

- [ ] **Step 1: Create 5.1 经验与等级**

File: `src/features/web/docs/topics/progress/exp-levels.tsx`

Primitives: `DocSection`, `DocKeyValue`, `DocCallout`

Sections:
- `id="exp-sources"` "经验从哪里来" — 1 paragraph: 闯关完成（正确率 ≥ 60%）、每日挑战、连击奖励. Call out the 60% threshold explicitly in the prose.
- `id="levels"` "等级范围" — `DocKeyValue`:
  - `{ key: "起始等级", value: "Lv. 0" }`
  - `{ key: "最高等级", value: "Lv. 100" }`
  - `{ key: "Lv. 0 → Lv. 1", value: "100 EXP（固定入门成本）" }`
  - `{ key: "Lv. 2 起", value: "base × 1.05^(n−2)", note: "指数曲线，越高越难升" }`
  - `{ key: "满级累计 EXP", value: "约 248,531" }`
- `id="view"` "在哪里查看" — 1 paragraph: 个人中心、排行榜、Dashboard 数据行.
- `id="callout"` add `DocCallout variant="info" title="为什么要 60% 正确率"` explaining this prevents gaming the system.

**Description:** "经验值怎么算，Lv. 0 到 Lv. 100 的完整成长曲线，60% 正确率阈值规则。"

- [ ] **Step 2: Create 5.2 连击与评分**

File: `src/features/web/docs/topics/progress/combo-rating.tsx`

Primitives: `DocSection`, `DocKeyValue`, `DocCompareTable`

Sections:
- `id="combo"` "连击奖励" — 1 paragraph intro, then `DocKeyValue`:
  - `{ key: "3 连击", value: "额外 +3 分" }`
  - `{ key: "5 连击", value: "额外 +5 分" }`
  - `{ key: "10 连击", value: "额外 +10 分" }`
- `id="rating"` "四档评分" — 1 paragraph intro, then `DocCompareTable`:
  - `columns={["阈值", "颜色"]}`, `labelHeader="等级"`
  - rows: `优秀` → `["≥ 90%", "teal"]`; `良好` → `["≥ 70%", "blue"]`; `及格` → `["≥ 60%", "amber"]`; `继续加油` → `["< 60%", "rose"]`
- `id="impact"` "评分对 EXP 的影响" — 1 paragraph: 只有达到 60% 及格线才会发放经验奖励.

**Description:** "3 / 5 / 10 连击的额外奖励规则，和四档评分（优秀 / 良好 / 及格 / 继续加油）。"

- [ ] **Step 3: Create 5.3 连续打卡**

File: `src/features/web/docs/topics/progress/play-streak.tsx`

Primitives: `DocSection`, `DocCallout`, `DocKeyValue`

Sections:
- `id="what"` "什么是连续打卡" — 2 paragraphs: play streak 是连续每天都有学习记录的天数，一旦中断会重置.
- `id="update-rules"` "每日更新规则" — 1 paragraph then `DocKeyValue`:
  - `{ key: "昨天有学习", value: "streak + 1" }`
  - `{ key: "前天或更早学习", value: "重置为 1" }`
  - `{ key: "今天已学习", value: "当天不再变动" }`
- `id="when"` "什么时候更新" — 1 paragraph: 每天凌晨 2 点自动跑定时任务更新全站 streak.
- `id="max"` "历史最高" — 1 short paragraph: max_play_streak 记录你的最长连续天数，即使现在中断了也不会被覆盖.
- Add a final `DocCallout variant="tip"` reminding users that 保持连续不需要玩很久，完成一次任意学习动作即可.

**Description:** "每日连续打卡规则：什么时候加、什么时候重置，定时任务什么时候跑。"

- [ ] **Step 4: Create 5.4 排行榜**

File: `src/features/web/docs/topics/progress/leaderboard.tsx`

Primitives: `DocSection`, `DocFeatureGrid`, `DocCompareTable`

Sections:
- `id="what"` "排行榜维度" — 2 paragraphs: 斗学排行榜有 2 个指标 × 3 个时段 = 6 张榜.
- `id="metrics"` "两个指标" — `DocFeatureGrid columns={2}`:
  - `{ title: "经验值 EXP", desc: "按一段时间内获得的经验总和排序" }`
  - `{ title: "游戏时长 playtime", desc: "按一段时间内的学习秒数排序" }`
- `id="periods"` "三个时段" — 1 paragraph listing: 日榜、周榜、月榜，边界按自然日/自然周/自然月切分.
- `id="display"` "榜单如何展示" — 1 paragraph: Top 3 上榜台、4+ 列表、展示前 100 名、&ldquo;我的排名&rdquo;悬浮显示.

**Description:** "排行榜的 6 种组合：EXP vs playtime × 日/周/月。前 100 名展示。"

- [ ] **Step 5: Update registry**

Edit `src/features/web/docs/registry.ts`:

1. Add imports:
```ts
import { TrendingUp } from "lucide-react";
import ExpLevels from "./topics/progress/exp-levels";
import ComboRating from "./topics/progress/combo-rating";
import PlayStreak from "./topics/progress/play-streak";
import Leaderboard from "./topics/progress/leaderboard";
```

2. Append new category:
```ts
{
  slug: "progress",
  title: "成长与激励",
  description: "经验、等级、连击、打卡、排行榜——看得见的成长路径。",
  icon: TrendingUp,
  accentClass: "border-amber-200 bg-amber-50 text-amber-600",
  topics: [
    {
      slug: "exp-levels",
      title: "经验与等级",
      description: "经验来源、Lv. 0 → Lv. 100 成长曲线、60% 正确率阈值。",
      Component: ExpLevels,
    },
    {
      slug: "combo-rating",
      title: "连击与评分",
      description: "连击奖励和四档评分（优秀 / 良好 / 及格 / 继续加油）。",
      Component: ComboRating,
    },
    {
      slug: "play-streak",
      title: "连续打卡",
      description: "streak 每日更新规则和历史最高记录。",
      Component: PlayStreak,
    },
    {
      slug: "leaderboard",
      title: "排行榜",
      description: "EXP / playtime × 日 / 周 / 月 的六种榜单组合。",
      Component: Leaderboard,
    },
  ],
},
```

- [ ] **Step 6: Typecheck**

Run: `cd dx-web && npx tsc --noEmit 2>&1 | tail -20`
Expected: zero output.

- [ ] **Step 7: Commit**

```bash
cd dx-web && git add src/features/web/docs/topics/progress/ src/features/web/docs/registry.ts && git commit -m "feat(docs): write progress category (exp/combo/streak/leaderboard)"
```

---

## Task 16: Content — 斗学社与好友 (4 topics)

**Spec reference:** lines 83–88.

**Files:**
- Create: `src/features/web/docs/topics/community/posts-comments.tsx`
- Create: `src/features/web/docs/topics/community/likes-follows.tsx`
- Create: `src/features/web/docs/topics/community/feeds.tsx`
- Create: `src/features/web/docs/topics/community/profile-view.tsx`
- Modify: `src/features/web/docs/registry.ts`

- [ ] **Step 1: Create 6.1 发帖与评论**

Primitives: `DocSection`, `DocKeyValue`, `DocCallout`

Sections:
- `id="post"` "发一条帖子" — 1 paragraph describing the post composer entry.
- `id="post-rules"` "帖子规则" — `DocKeyValue`:
  - `{ key: "正文长度", value: "最多 2000 字" }`
  - `{ key: "图片", value: "可选，单张；上传后与帖子绑定" }`
  - `{ key: "标签", value: "最多 5 个，每个最多 20 字" }`
- `id="comments"` "评论" — 1 paragraph explaining: 评论最多 500 字；斗学社不支持嵌套回复（评论不能回复评论）. Use `DocCallout variant="info"` to highlight the "no nested replies" rule.
- `id="edit-delete"` "编辑和删除" — 1 paragraph: 自己的帖子和评论可以随时编辑和删除，删除是软删除（其他人不再看到，但数据库里保留）.

**Description:** "帖子正文、图片、标签的规则，以及评论为什么不能嵌套。"

- [ ] **Step 2: Create 6.2 点赞、收藏与关注**

Primitives: `DocSection`, `DocCallout`

Sections:
- `id="like"` "点赞" — 1 paragraph: 任意帖子都可以点赞 / 取消点赞，这是一个 toggle 动作.
- `id="bookmark"` "收藏帖子" — 1 paragraph: 收藏后可以在&ldquo;我收藏的&rdquo;流里找到.
- `id="follow"` "关注用户" — 1 paragraph: 关注是单向的（不是互关）. 关注后可以在&ldquo;关注中&rdquo;流里看到对方的帖子. 不能关注自己. Use `DocCallout variant="info"` to flag the "不能关注自己" rule.

**Description:** "点赞、收藏、关注三个动作的规则和它们之间的区别。"

- [ ] **Step 3: Create 6.3 社区动态流**

Primitives: `DocSection`, `DocFeatureGrid`

Sections:
- `id="tabs"` "四个 tab" — `DocFeatureGrid columns={2}`:
  - `{ title: "最新 (latest)", desc: "按发布时间倒序" }`
  - `{ title: "热门 (hot)", desc: "按热度排序的精选内容" }`
  - `{ title: "关注中 (following)", desc: "只显示你关注的人发的帖子" }`
  - `{ title: "我收藏的 (bookmarked)", desc: "你收藏过的帖子" }`
- `id="pagination"` "无限滚动 + 游标分页" — 1 paragraph explaining the feed uses cursor-based pagination so scrolling loads more in place without page numbers.

**Description:** "斗学社有四种内容流视角，外加无限滚动和游标分页。"

- [ ] **Step 4: Create 6.4 个人主页与粉丝**

Primitives: `DocSection`, `DocKeyValue`

Sections:
- `id="access"` "进入个人主页" — 1 paragraph: 点击帖子作者头像进入.
- `id="shows"` "个人主页展示什么" — `DocKeyValue`:
  - `{ key: "昵称", value: "用户的显示名" }`
  - `{ key: "简介", value: "用户自己填写的介绍" }`
  - `{ key: "等级与经验", value: "当前 Lv. 和 EXP 进度" }`
  - `{ key: "连续打卡", value: "当前和历史最高天数" }`
  - `{ key: "关注 / 粉丝", value: "双向计数" }`
- `id="posts"` "对方的帖子" — 1 paragraph: 个人主页还会展示这个人发过的帖子流.

**Description:** "从帖子头像进入个人主页，看到对方的等级、打卡、关注、帖子。"

- [ ] **Step 5: Update registry**

Edit `src/features/web/docs/registry.ts`:

1. Add imports:
```ts
import { MessageCircle } from "lucide-react";
import PostsComments from "./topics/community/posts-comments";
import LikesFollows from "./topics/community/likes-follows";
import Feeds from "./topics/community/feeds";
import ProfileView from "./topics/community/profile-view";
```

2. Append new category:
```ts
{
  slug: "community",
  title: "斗学社与好友",
  description: "斗学社是用户之间交流的社区：发帖、评论、点赞、关注、个人主页。",
  icon: MessageCircle,
  accentClass: "border-pink-200 bg-pink-50 text-pink-600",
  topics: [
    {
      slug: "posts-comments",
      title: "发帖与评论",
      description: "帖子正文、图片、标签的规则，以及评论为什么不能嵌套。",
      Component: PostsComments,
    },
    {
      slug: "likes-follows",
      title: "点赞、收藏与关注",
      description: "三个动作的规则和它们之间的区别。",
      Component: LikesFollows,
    },
    {
      slug: "feeds",
      title: "社区动态流",
      description: "最新 / 热门 / 关注中 / 我收藏的四种视角。",
      Component: Feeds,
    },
    {
      slug: "profile-view",
      title: "个人主页与粉丝",
      description: "看对方的等级、打卡、关注数和帖子流。",
      Component: ProfileView,
    },
  ],
},
```

- [ ] **Step 6: Typecheck**

Run: `cd dx-web && npx tsc --noEmit 2>&1 | tail -20`
Expected: zero output.

- [ ] **Step 7: Commit**

```bash
cd dx-web && git add src/features/web/docs/topics/community/ src/features/web/docs/registry.ts && git commit -m "feat(docs): write community category"
```

---

## Task 17: Content — 学习小组 (5 topics)

**Spec reference:** lines 90–96.

**Files:**
- Create: `src/features/web/docs/topics/groups/overview.tsx`
- Create: `src/features/web/docs/topics/groups/create-join.tsx`
- Create: `src/features/web/docs/topics/groups/members-applications.tsx`
- Create: `src/features/web/docs/topics/groups/subgroups.tsx`
- Create: `src/features/web/docs/topics/groups/start-game.tsx`
- Modify: `src/features/web/docs/registry.ts`

- [ ] **Step 1: Create 7.1 什么是学习小组**

Primitives: `DocSection`, `DocKeyValue`, `DocCallout`, `DocLink`

Sections:
- `id="what"` "学习小组的定位" — 2 paragraphs describing the concept.
- `id="limits"` "硬限制" — `DocKeyValue`:
  - `{ key: "单个小组成员上限", value: "50 人" }`
  - `{ key: "单个小组子分组上限", value: "10 个" }`
  - `{ key: "创建小组", value: "需要 VIP 会员" }`
- `id="two-formats"` "两种玩法" — 1 paragraph: solo / team, link to `/docs/learning-modes/group-mode`.
- Add `DocCallout variant="warning" title="创建小组需要 VIP"`.

**Description:** "小组是一群学习伙伴的空间。介绍小组的硬限制、玩法和 VIP 要求。"

- [ ] **Step 2: Create 7.2 创建与加入**

Primitives: `DocSection`, `DocSteps`, `DocCallout`, `DocSlug`

Sections:
- `id="create"` "创建小组" — `DocSteps`:
  - `{ title: "确认你是 VIP", desc: "免费用户不能创建小组" }`
  - `{ title: "填写基本信息", desc: "名称、可选描述" }`
  - `{ title: "生成邀请码和二维码", desc: "系统自动为你的小组生成一个独一无二的邀请码" }`
- `id="join"` "加入小组" — 1 paragraph: 三种方式——邀请码、二维码、申请加入.
- `id="invite-link"` "邀请链接格式" — 1 paragraph referencing that invite links are `<DocSlug>/g/[code]</DocSlug>` format.
- `id="leave"` "退出小组" — 1 paragraph + `DocCallout variant="warning"`: 组主不能退出自己的小组，必须解散或转让（目前仅支持解散）.

**Description:** "创建小组的流程，三种加入方式，以及组主为什么不能退出。"

- [ ] **Step 3: Create 7.3 成员与申请管理**

Primitives: `DocSection`, `DocFeatureGrid`, `DocCallout`

Sections:
- `id="owner-view"` "组主视角" — 1 paragraph: 组主能看到成员列表和申请列表.
- `id="member-list"` "成员管理" — 1 paragraph: 组主可以踢出成员、查看成员学习进度.
- `id="applications"` "申请管理" — 1 paragraph: 收到申请后组主可以接受或拒绝，申请可以包含一条留言.
- `id="roles"` "成员角色" — 1 paragraph: 小组只有两级角色——组主（owner）和成员（member），没有中间管理层.

**Description:** "组主如何管理成员和申请；为什么小组只有两级角色。"

- [ ] **Step 4: Create 7.4 子分组 (Subgroups)**

Primitives: `DocSection`, `DocCallout`, `DocKeyValue`

Sections:
- `id="what"` "子分组是什么" — 1 paragraph: 子分组用于 group_team 分组对战模式；每个小组最多 10 个子分组.
- `id="rules"` "子分组规则" — `DocKeyValue`:
  - `{ key: "每组子分组上限", value: "10 个" }`
  - `{ key: "成员归属", value: "一个成员最多属于一个子分组" }`
- `id="assign"` "如何分配成员" — 1 paragraph: 组主在小组页面为每个子分组选择成员.
- `id="when"` "什么时候用得上" — `DocCallout variant="info"`: 只有使用 group_team 开局才会真正用到子分组.

**Description:** "子分组用于 group_team 分组对战模式，每个小组最多 10 个子分组。"

- [ ] **Step 5: Create 7.5 开局与推进游戏**

Primitives: `DocSection`, `DocSteps`, `DocFlow`, `DocCallout`, `DocLink`

Sections:
- `id="who"` "谁能开局" — 1 paragraph: 只有组主可以开局小组游戏.
- `id="flow"` "开局四步走" — `DocSteps`:
  - `{ title: "选游戏", desc: "在小组页选择这次要玩的游戏" }`
  - `{ title: "选模式", desc: "group_solo 或 group_team" }`
  - `{ title: "设置起始关卡", desc: "所有组员将从这关开始" }`
  - `{ title: "开始游戏", desc: "组员进入房间后同步开始" }`
- `id="during"` "推进到下一关" — 1 paragraph: 组主可以在当前关卡结束后点击&ldquo;下一关&rdquo;继续推进；也可以随时&ldquo;强制结束&rdquo;整局.
- `id="results"` "结算展示" — 1 paragraph: 小组排名 + 每位组员的个人分数，team 模式会多一层分组汇总.

**Description:** "组主开局流程、推进下一关、强制结束，以及结算展示。"

- [ ] **Step 6: Update registry**

Edit `src/features/web/docs/registry.ts`:

1. Add imports:
```ts
import { Users2 } from "lucide-react";
import GroupsOverview from "./topics/groups/overview";
import CreateJoin from "./topics/groups/create-join";
import MembersApplications from "./topics/groups/members-applications";
import Subgroups from "./topics/groups/subgroups";
import StartGame from "./topics/groups/start-game";
```

2. Append new category:
```ts
{
  slug: "groups",
  title: "学习小组",
  description: "和朋友组队学习：创建、加入、管理、开局小组游戏。",
  icon: Users2,
  accentClass: "border-violet-200 bg-violet-50 text-violet-600",
  topics: [
    {
      slug: "overview",
      title: "什么是学习小组",
      description: "小组的定位、硬限制（50 人 / 10 子分组）、VIP 要求。",
      Component: GroupsOverview,
    },
    {
      slug: "create-join",
      title: "创建与加入",
      description: "创建流程、三种加入方式、组主不能退出的规则。",
      Component: CreateJoin,
    },
    {
      slug: "members-applications",
      title: "成员与申请管理",
      description: "组主如何管理成员和申请；owner / member 两级角色。",
      Component: MembersApplications,
    },
    {
      slug: "subgroups",
      title: "子分组 (Subgroups)",
      description: "用于 group_team 分组对战模式，每组最多 10 个子分组。",
      Component: Subgroups,
    },
    {
      slug: "start-game",
      title: "开局与推进游戏",
      description: "组主开局四步走、推进下一关、强制结束、结算。",
      Component: StartGame,
    },
  ],
},
```

- [ ] **Step 7: Typecheck**

Run: `cd dx-web && npx tsc --noEmit 2>&1 | tail -20`
Expected: zero output.

- [ ] **Step 8: Commit**

```bash
cd dx-web && git add src/features/web/docs/topics/groups/ src/features/web/docs/registry.ts && git commit -m "feat(docs): write groups category"
```

---

## Task 18: Content — AI 智能学习 (2 topics)

**Spec reference:** lines 98–101.

**Files:**
- Create: `src/features/web/docs/topics/ai/ai-custom-sentence.tsx`
- Create: `src/features/web/docs/topics/ai/ai-custom-vocab.tsx`
- Modify: `src/features/web/docs/registry.ts`

- [ ] **Step 1: Create 8.1 AI 随心学（句子）**

Primitives: `DocSection`, `DocCallout`, `DocFlow`, `DocKeyValue`, `DocSlug`, `DocLink`

Sections:
- `id="what"` "AI 随心学是什么" — 2 paragraphs: 给几个关键词，让 AI 帮你生成英语故事和学习内容.
- `id="inputs"` "输入参数" — `DocKeyValue`:
  - `{ key: "关键词", value: "3–10 个英文或中文关键词" }`
  - `{ key: "难度 (CEFR)", value: "a1-a2 / b1-b2 / c1-c2" }`
- `id="flow"` "四步流程" — `DocFlow`:
  - `[{ label: "generate", desc: "生成原始故事" }, { label: "format", desc: "格式化句子" }, { label: "break", desc: "拆分单元" }, { label: "gen items", desc: "生成练习题" }]`
- `id="cost"` "能量豆消耗" — 1 paragraph: 每一步消耗能量豆；生成失败时自动退还；详见 <DocLink href="/docs/membership/beans-monthly">能量豆</DocLink>.
- `id="callout"` `DocCallout variant="warning" title="仅 VIP 可用"`.
- `id="after"` "生成完成后" — 1 paragraph: 生成的内容进入你的&ldquo;我创建的游戏&rdquo;列表，可以继续编辑、发布.

**Description:** "给 AI 一组关键词和难度，让它生成完整的英语学习内容。按四步流程、消耗能量豆。"

- [ ] **Step 2: Create 8.2 AI 随心学（词汇）**

Primitives: `DocSection`, `DocKeyValue`, `DocCallout`, `DocFlow`

Sections:
- `id="what"` "词汇变体" — 1 paragraph: 与句子版相比，词汇版让 AI 生成词汇列表而不是完整句子，适合快速建立词汇课程.
- `id="flow"` "同样的四步流程" — 1 paragraph + `DocFlow` (same 4 steps as sentence variant).
- `id="game-limits"` "不同游戏类型的数量上限" — 1 paragraph intro + `DocKeyValue`:
  - `{ key: "vocab-match（配对）", value: "每关 5 对" }`
  - `{ key: "vocab-elimination（消消乐）", value: "每关 8 对" }`
  - `{ key: "vocab-battle（对轰）", value: "每关 20 对" }`
- `id="level-limits"` "每课上限" — `DocKeyValue`:
  - `{ key: "每关卡元数据上限", value: "20 个" }`
  - `{ key: "每课关卡上限", value: "20 个" }`

**Description:** "词汇变体的 AI 生成：四步流程相同，但按游戏类型限制了每关的词对数。"

- [ ] **Step 3: Update registry**

Edit `src/features/web/docs/registry.ts`:

1. Add imports:
```ts
import { Sparkles } from "lucide-react";
import AiCustomSentence from "./topics/ai/ai-custom-sentence";
import AiCustomVocab from "./topics/ai/ai-custom-vocab";
```

2. Append new category:
```ts
{
  slug: "ai",
  title: "AI 智能学习",
  description: "让 AI 根据你的关键词和难度生成课程，消耗能量豆，失败退还。",
  icon: Sparkles,
  accentClass: "border-purple-200 bg-purple-50 text-purple-600",
  topics: [
    {
      slug: "ai-custom-sentence",
      title: "AI 随心学（句子）",
      description: "让 AI 生成完整英语故事：四步流程、CEFR 难度、能量豆消耗。",
      Component: AiCustomSentence,
    },
    {
      slug: "ai-custom-vocab",
      title: "AI 随心学（词汇）",
      description: "词汇变体：四步流程一致，按游戏类型限制每关词对数。",
      Component: AiCustomVocab,
    },
  ],
},
```

- [ ] **Step 4: Typecheck**

Run: `cd dx-web && npx tsc --noEmit 2>&1 | tail -20`
Expected: zero output.

- [ ] **Step 5: Commit**

```bash
cd dx-web && git add src/features/web/docs/topics/ai/ src/features/web/docs/registry.ts && git commit -m "feat(docs): write ai category (sentence + vocab variants)"
```

---

## Task 19: Content — 创作课程 (5 topics)

**Spec reference:** lines 103–109.

**Files:**
- Create: `src/features/web/docs/topics/creation/creator-intro.tsx`
- Create: `src/features/web/docs/topics/creation/new-course.tsx`
- Create: `src/features/web/docs/topics/creation/levels-units.tsx`
- Create: `src/features/web/docs/topics/creation/content-items.tsx`
- Create: `src/features/web/docs/topics/creation/publish-withdraw.tsx`
- Modify: `src/features/web/docs/registry.ts`

- [ ] **Step 1: Create 9.1 创作者入门**

Primitives: `DocSection`, `DocFeatureGrid`, `DocFlow`, `DocCallout`

Sections:
- `id="why"` "为什么要创作" — 1 paragraph on motivations (自己整理 / 分享好友 / 私人定制).
- `id="structure"` "课程结构" — 1 paragraph describing the hierarchy: 课程（course）→ 关卡（level）→ 单元（metadata）→ 内容条目（content item）. Follow with `DocFlow` showing the 4-level nesting.
- `id="paths"` "两种创建路径" — `DocFeatureGrid columns={2}`:
  - `{ title: "从零手动创建", desc: "自己写每一条内容条目" }`
  - `{ title: "AI 生成", desc: "让 AI 根据关键词帮你生成" }`
- `id="lifecycle"` "生命周期" — `DocFlow`: `[{ label: "draft", desc: "草稿" }, { label: "published", desc: "已发布" }, { label: "withdraw", desc: "已撤回" }]`

**Description:** "概览：为什么创作、课程层级结构、两种创建路径、发布生命周期。"

- [ ] **Step 2: Create 9.2 新建课程**

Primitives: `DocSection`, `DocSteps`, `DocKeyValue`, `DocCallout`

Sections:
- `id="entry"` "创建入口" — 1 paragraph: 在&ldquo;我的游戏&rdquo;页点&ldquo;新建&rdquo;.
- `id="fields"` "基本信息" — `DocKeyValue`:
  - `{ key: "名称", value: "必填" }`
  - `{ key: "描述", value: "可选" }`
  - `{ key: "封面图", value: "可选，上传规则见下" }`
  - `{ key: "分类", value: "从分类树中选择" }`
  - `{ key: "出版社", value: "可选" }`
  - `{ key: "模式", value: "四选一" }`
- `id="cover"` "封面上传规则" — `DocCallout variant="warning"` listing: 单张 ≤ 2MB，仅支持 JPEG / PNG.
- `id="next"` "创建完成后" — 1 paragraph: 创建成功后进入课程编辑页，开始添加关卡和单元.

**Description:** "新建课程的基本字段、封面上传规则（≤2MB, JPEG/PNG）。"

- [ ] **Step 3: Create 9.3 关卡与单元**

Primitives: `DocSection`, `DocSteps`, `DocCallout`

Sections:
- `id="level"` "关卡是可玩单位" — 1 paragraph: 关卡是学习者实际在游戏里闯的单位.
- `id="metadata"` "单元是关卡内的内容块" — 1 paragraph: 每个关卡包含一个或多个单元（metadata），单元再拆成具体的内容条目.
- `id="manage"` "增加、修改、重排、删除" — 1 paragraph describing all these operations exist.
- `id="content-types"` "内容类型限制" — `DocCallout variant="info"` listing: word / block / phrase / sentence 四种内容类型，难度越高可选范围越窄.

**Description:** "关卡是可玩单位，单元是关卡内的内容块，支持增改重排删和内容类型限制。"

- [ ] **Step 4: Create 9.4 内容条目（题目）**

Primitives: `DocSection`, `DocSteps`, `DocFeatureGrid`, `DocCallout`

Sections:
- `id="what"` "内容条目是最小单位" — 1 paragraph.
- `id="add-methods"` "三种添加方式" — `DocFeatureGrid columns={3}`:
  - `{ title: "手动添加", desc: "逐条输入或粘贴" }`
  - `{ title: "AI 生成", desc: "让 AI 按单元主题生成" }`
  - `{ title: "从单元分解", desc: "将一段长文本拆成多条" }`
- `id="reorder"` "批量重排" — 1 paragraph: 支持拖拽重排.
- `id="delete"` "删除" — 1 paragraph: 单个删除 / 整关清空.

**Description:** "内容条目的添加、重排和删除。三种添加方式：手动、AI 生成、从单元分解。"

- [ ] **Step 5: Create 9.5 发布与撤回**

Primitives: `DocSection`, `DocSteps`, `DocFlow`, `DocCallout`

Sections:
- `id="checklist"` "发布前检查" — 1 paragraph: 关卡、单元、内容条目齐全.
- `id="publish"` "发布" — 1 paragraph: 发布后进入游戏广场，其他用户可以搜到并玩.
- `id="withdraw"` "撤回" — 1 paragraph: 撤回是下架不是删除，随时可以重新发布.
- `id="lifecycle"` "生命周期" — `DocFlow` showing draft → published → withdraw → (可再发布回 published).

**Description:** "发布流程、撤回规则和草稿 → 发布 → 撤回的生命周期。"

- [ ] **Step 6: Update registry**

Edit `src/features/web/docs/registry.ts`:

1. Add imports:
```ts
import { PencilLine } from "lucide-react";
import CreatorIntro from "./topics/creation/creator-intro";
import NewCourse from "./topics/creation/new-course";
import LevelsUnits from "./topics/creation/levels-units";
import ContentItems from "./topics/creation/content-items";
import PublishWithdraw from "./topics/creation/publish-withdraw";
```

2. Append new category:
```ts
{
  slug: "creation",
  title: "创作课程",
  description: "自己动手做一套学习内容：新建、加关卡、加内容、发布、撤回。",
  icon: PencilLine,
  accentClass: "border-sky-200 bg-sky-50 text-sky-600",
  topics: [
    {
      slug: "creator-intro",
      title: "创作者入门",
      description: "为什么创作、课程层级结构、两种创建路径。",
      Component: CreatorIntro,
    },
    {
      slug: "new-course",
      title: "新建课程",
      description: "基本信息字段和封面上传规则（≤2MB, JPEG/PNG）。",
      Component: NewCourse,
    },
    {
      slug: "levels-units",
      title: "关卡与单元",
      description: "关卡和单元（metadata）的关系，内容类型限制。",
      Component: LevelsUnits,
    },
    {
      slug: "content-items",
      title: "内容条目（题目）",
      description: "添加、重排和删除内容条目的三种方式。",
      Component: ContentItems,
    },
    {
      slug: "publish-withdraw",
      title: "发布与撤回",
      description: "发布前检查、生命周期（draft / published / withdraw）。",
      Component: PublishWithdraw,
    },
  ],
},
```

- [ ] **Step 7: Typecheck**

Run: `cd dx-web && npx tsc --noEmit 2>&1 | tail -20`
Expected: zero output.

- [ ] **Step 8: Commit**

```bash
cd dx-web && git add src/features/web/docs/topics/creation/ src/features/web/docs/registry.ts && git commit -m "feat(docs): write creation category"
```

---

## Task 20: Content — 会员与能量豆 (5 topics)

**Spec reference:** lines 111–117.

**Files:**
- Create: `src/features/web/docs/topics/membership/tiers-compare.tsx`
- Create: `src/features/web/docs/topics/membership/benefits.tsx`
- Create: `src/features/web/docs/topics/membership/purchase-flow.tsx`
- Create: `src/features/web/docs/topics/membership/beans-packages.tsx`
- Create: `src/features/web/docs/topics/membership/beans-monthly.tsx`
- Modify: `src/features/web/docs/registry.ts`

- [ ] **Step 1: Create 10.1 会员等级对比**

Primitives: `DocSection`, `DocCompareTable`, `DocCallout`

Sections:
- `id="five-tiers"` "五个等级" — 1 paragraph intro.
- `id="compare"` "完整对比" — `DocCompareTable`:
  - `columns={["免费", "月度 ¥39", "季度 ¥99", "年度 ¥309", "终身 ¥1999"]}`
  - `labelHeader="权益"`
  - rows: `有效期` → `["—", "1 个月", "3 个月", "12 个月", "永久"]`; `全部关卡` → `[false, true, true, true, true]`; `PK 对战` → `[false, true, true, true, true]`; `创建学习小组` → `[false, true, true, true, true]`; `AI 随心学` → `[false, true, true, true, true]`; `每月能量豆` → `["—", "10,000", "10,000", "10,000", "15,000"]`

**Description:** "五档会员价格、有效期和完整权益对比表。"

- [ ] **Step 2: Create 10.2 会员权益**

Primitives: `DocSection`, `DocFeatureGrid`

Sections:
- `id="benefits"` "会员解锁的能力" — `DocFeatureGrid columns={2}`:
  - `{ title: "全部关卡", desc: "免费用户只能玩每个游戏的第一关" }`
  - `{ title: "PK 对战", desc: "所有 PK 模式需要 VIP" }`
  - `{ title: "创建学习小组", desc: "小组创建需要 VIP；加入是免费的" }`
  - `{ title: "AI 随心学", desc: "两种 AI 生成都需要 VIP" }`
  - `{ title: "每月能量豆赠送", desc: "月 / 季 / 年会员每月 10,000 豆，终身 15,000" }`
  - `{ title: "学习服务支持", desc: "中高等级会员享有优先支持" }`

**Description:** "会员解锁的全部能力：关卡、PK、小组、AI、能量豆赠送、支持。"

- [ ] **Step 3: Create 10.3 购买流程**

Primitives: `DocSection`, `DocSteps`, `DocFlow`, `DocCallout`

Sections:
- `id="steps"` "五步完成购买" — `DocSteps`:
  - `{ title: "选等级", desc: "在购买页选择月 / 季 / 年 / 终身" }`
  - `{ title: "生成订单", desc: "确认后系统创建订单" }`
  - `{ title: "选支付方式", desc: "微信支付或支付宝" }`
  - `{ title: "扫码完成支付", desc: "按提示扫描二维码完成支付" }`
  - `{ title: "权益到账", desc: "后端自动将会员权益写入你的账户" }`
- `id="lifecycle"` "订单状态流转" — `DocFlow`: `[{ label: "pending", desc: "待支付" }, { label: "paid", desc: "已支付" }, { label: "fulfilled", desc: "已发放" }]`
- `id="expire"` "30 分钟过期" — `DocCallout variant="warning"`: 订单创建后 30 分钟内未支付会自动过期，需要重新生成.

**Description:** "会员购买五步流程、订单状态流转、30 分钟过期规则。"

- [ ] **Step 4: Create 10.4 能量豆购买**

Primitives: `DocSection`, `DocCompareTable`, `DocCallout`

Sections:
- `id="what-for"` "能量豆是什么" — 1 paragraph: 能量豆是斗学的软通货，主要用于 AI 随心学的生成、格式化、分解、题目生成等动作.
- `id="packages"` "五档充值包" — `DocCompareTable`:
  - `columns={["价格", "基础豆", "赠送豆", "合计"]}`
  - `labelHeader="档位"`
  - rows: `1 元包` → `["¥1", "1,000", "0", "1,000"]`; `5 元包` → `["¥5", "5,000", "0", "5,000"]`; `10 元包` → `["¥10", "10,000", "1,000", "11,000"]`; `50 元包` → `["¥50", "50,000", "7,500", "57,500"]`; `100 元包` → `["¥100", "100,000", "20,000", "120,000"]`
- `id="tags"` "标签" — 1 paragraph: 10 元包有&ldquo;超值推荐&rdquo;标签，100 元包有&ldquo;最划算&rdquo;标签.

**Description:** "能量豆是软通货，用于 AI 生成。五档充值包的价格和赠送比例。"

- [ ] **Step 5: Create 10.5 月度赠送与清零**

Primitives: `DocSection`, `DocKeyValue`, `DocCallout`

Sections:
- `id="grants"` "每月赠送多少" — `DocKeyValue`:
  - `{ key: "月 / 季 / 年会员", value: "每月 10,000 豆" }`
  - `{ key: "终身会员", value: "每月 15,000 豆" }`
  - `{ key: "免费用户 / 过期会员", value: "没有赠送" }`
- `id="when"` "赠送日是哪天" — 1 paragraph: 按你首次付费的月份对应日，例如 3 月 15 日首次付费，则每月 15 日赠送一次.
- `id="month-end"` "月末日期特殊处理" — `DocCallout variant="info"`: 如果原赠送日是 29 / 30 / 31 号，遇到更短的月份（如 2 月）则顺延到该月最后一天.
- `id="reset"` "月度清零机制" — 1 paragraph: 新一月到账前会先清零上月剩余的&ldquo;赠送&rdquo;豆（购买的豆不清零）. 这是为了让每月赠送的豆能用在当月.
- `id="expired"` "过期会员" — 1 paragraph: 会员过期后不再赠送，但已购买的豆可以继续使用.

**Description:** "月度赠送节奏（月/季/年 10k，终身 15k）、赠送日规则、月末特殊处理、清零机制。"

- [ ] **Step 6: Update registry**

Edit `src/features/web/docs/registry.ts`:

1. Add imports:
```ts
import { Crown } from "lucide-react";
import TiersCompare from "./topics/membership/tiers-compare";
import Benefits from "./topics/membership/benefits";
import PurchaseFlow from "./topics/membership/purchase-flow";
import BeansPackages from "./topics/membership/beans-packages";
import BeansMonthly from "./topics/membership/beans-monthly";
```

2. Append new category:
```ts
{
  slug: "membership",
  title: "会员与能量豆",
  description: "会员五档对比、权益、购买流程，以及能量豆的购买和月度赠送。",
  icon: Crown,
  accentClass: "border-yellow-200 bg-yellow-50 text-yellow-600",
  topics: [
    {
      slug: "tiers-compare",
      title: "会员等级对比",
      description: "五档价格、有效期、完整权益对比表。",
      Component: TiersCompare,
    },
    {
      slug: "benefits",
      title: "会员权益",
      description: "解锁全部关卡、PK、小组创建、AI 随心学、能量豆赠送、支持。",
      Component: Benefits,
    },
    {
      slug: "purchase-flow",
      title: "购买流程",
      description: "五步完成购买、订单状态流转、30 分钟过期规则。",
      Component: PurchaseFlow,
    },
    {
      slug: "beans-packages",
      title: "能量豆购买",
      description: "五档充值包的价格和赠送比例。",
      Component: BeansPackages,
    },
    {
      slug: "beans-monthly",
      title: "月度赠送与清零",
      description: "月度赠送节奏、赠送日规则、月末特殊处理、清零机制。",
      Component: BeansMonthly,
    },
  ],
},
```

- [ ] **Step 7: Typecheck**

Run: `cd dx-web && npx tsc --noEmit 2>&1 | tail -20`
Expected: zero output.

- [ ] **Step 8: Commit**

```bash
cd dx-web && git add src/features/web/docs/topics/membership/ src/features/web/docs/registry.ts && git commit -m "feat(docs): write membership category"
```

---

## Task 21: Content — 邀请与兑换 (3 topics)

**Spec reference:** lines 119–123.

**Files:**
- Create: `src/features/web/docs/topics/invites/referral-program.tsx`
- Create: `src/features/web/docs/topics/invites/invite-codes.tsx`
- Create: `src/features/web/docs/topics/invites/redeem-codes.tsx`
- Modify: `src/features/web/docs/registry.ts`

- [ ] **Step 1: Create 11.1 邀请好友赚佣金**

Primitives: `DocSection`, `DocKeyValue`, `DocFlow`, `DocCallout`

Sections:
- `id="page"` "推广页面" — 1 paragraph: 推广页展示你的专属邀请链接、二维码和关键统计数据.
- `id="stats"` "四项核心指标" — `DocKeyValue`:
  - `{ key: "累计佣金", value: "到目前为止赚到的全部佣金" }`
  - `{ key: "本月新增", value: "本月新增的邀请人数" }`
  - `{ key: "待激活", value: "已注册未付费的好友数量" }`
  - `{ key: "成功转化率", value: "付费转化比例" }`
- `id="success"` "怎么算推广成功" — 1 paragraph: 好友通过你的邀请链接注册并完成首次付费即视为推广成功.
- `id="status-flow"` "状态流转" — `DocFlow`: `[{ label: "pending", desc: "待激活" }, { label: "paid", desc: "已付费" }, { label: "rewarded", desc: "已发放" }]`
- `id="settle"` "佣金结算" — 1 paragraph: 佣金按时结算，可以申请提现.

**Description:** "推广页、四项核心统计、推广成功的定义，以及 pending → paid → rewarded 状态流。"

- [ ] **Step 2: Create 11.2 邀请码与群组码**

Primitives: `DocSection`, `DocCompareTable`, `DocSlug`

Sections:
- `id="two-codes"` "两种邀请码" — 1 paragraph: 斗学有两种邀请码——用户邀请码和群组邀请码.
- `id="compare"` "它们的区别" — `DocCompareTable`:
  - `columns={["用户邀请码", "群组邀请码"]}`
  - `labelHeader="维度"`
  - rows: `生成者` → `["每个用户一个", "小组创建时自动生成"]`; `作用` → `["新用户注册时建立推广关系", "被邀请者加入指定小组"]`; `链接格式` → `["ref cookie", "/g/[code]"]`
- `id="usage"` "如何使用" — 1 paragraph explaining: 你直接分享自己的推广链接给想邀请的人即可；如果是想让好友加入你的某个具体小组，用群组邀请码.

**Description:** "用户邀请码 vs 群组邀请码：生成者、作用、链接格式的区别。"

- [ ] **Step 3: Create 11.3 兑换码**

Primitives: `DocSection`, `DocSteps`, `DocCallout`

Sections:
- `id="what"` "兑换码是什么" — 1 paragraph: 兑换码是可以直接换成会员期限或能量豆的字符串，来源可能是活动、官方福利或客服补偿.
- `id="steps"` "使用兑换码" — `DocSteps`:
  - `{ title: "进入兑换码页面", desc: "在侧边栏&ldquo;兑换码&rdquo;入口" }`
  - `{ title: "粘贴或输入兑换码", desc: "区分大小写" }`
  - `{ title: "点击兑换", desc: "系统会实时校验和发放" }`
- `id="errors"` "兑换失败的常见原因" — `DocCallout variant="warning"` listing: 已被使用 / 兑换码不存在 / 已过期.

**Description:** "兑换码可换会员期限或能量豆。使用流程和常见失败原因。"

- [ ] **Step 4: Update registry**

Edit `src/features/web/docs/registry.ts`:

1. Add imports:
```ts
import { Gift } from "lucide-react";
import ReferralProgram from "./topics/invites/referral-program";
import InviteCodes from "./topics/invites/invite-codes";
import RedeemCodes from "./topics/invites/redeem-codes";
```

2. Append new category:
```ts
{
  slug: "invites",
  title: "邀请与兑换",
  description: "邀请好友赚佣金、区分两种邀请码、使用兑换码。",
  icon: Gift,
  accentClass: "border-orange-200 bg-orange-50 text-orange-600",
  topics: [
    {
      slug: "referral-program",
      title: "邀请好友赚佣金",
      description: "推广页、四项统计、状态流和佣金结算。",
      Component: ReferralProgram,
    },
    {
      slug: "invite-codes",
      title: "邀请码与群组码",
      description: "两种邀请码的区别和使用场景。",
      Component: InviteCodes,
    },
    {
      slug: "redeem-codes",
      title: "兑换码",
      description: "兑换码可换会员期限或能量豆。使用流程和失败原因。",
      Component: RedeemCodes,
    },
  ],
},
```

- [ ] **Step 5: Typecheck**

Run: `cd dx-web && npx tsc --noEmit 2>&1 | tail -20`
Expected: zero output.

- [ ] **Step 6: Commit**

```bash
cd dx-web && git add src/features/web/docs/topics/invites/ src/features/web/docs/registry.ts && git commit -m "feat(docs): write invites category"
```

---

## Task 22: Content — 账户与帮助 (5 topics, including FAQ)

**Spec reference:** lines 125–131.

**Files:**
- Create: `src/features/web/docs/topics/account/profile-edit.tsx`
- Create: `src/features/web/docs/topics/account/security.tsx`
- Create: `src/features/web/docs/topics/account/notices.tsx`
- Create: `src/features/web/docs/topics/account/feedback.tsx`
- Create: `src/features/web/docs/topics/account/faq.tsx`
- Modify: `src/features/web/docs/registry.ts`

- [ ] **Step 1: Create 12.1 个人资料**

Primitives: `DocSection`, `DocKeyValue`, `DocCallout`

Sections:
- `id="editable"` "可以编辑什么" — `DocKeyValue`:
  - `{ key: "昵称", value: "全站唯一" }`
  - `{ key: "城市", value: "自由填写" }`
  - `{ key: "简介", value: "自由文本" }`
  - `{ key: "头像", value: "上传图片（通过 imageId 绑定）" }`
- `id="username"` "用户名是只读的" — `DocCallout variant="info"`: 用户名在注册时确定，之后不能修改。如果你想展示不同的名字，修改昵称即可.
- `id="where-shown"` "在哪里会被看到" — 1 paragraph: 个人中心、排行榜、社区帖子、好友列表.

**Description:** "可编辑的资料字段和展示位置；用户名为什么是只读的。"

- [ ] **Step 2: Create 12.2 账号安全**

Primitives: `DocSection`, `DocSteps`, `DocCallout`

Sections:
- `id="change-email"` "修改邮箱" — `DocSteps`:
  - `{ title: "进入账号安全页", desc: "" }`
  - `{ title: "输入新邮箱", desc: "系统发送 6 位数字验证码到新邮箱" }`
  - `{ title: "验证", desc: "输入验证码完成切换" }`
- `id="change-password"` "修改密码" — 1 paragraph: 需要先输入当前密码，然后设置新密码.
- `id="logout"` "登出" — 1 paragraph: 登出会使当前 JWT 失效.
- `id="session-replaced"` "多设备登录" — `DocCallout variant="warning"` explaining: 斗学在每个账号仅允许一处活跃会话——在新设备登录会踢掉旧设备，旧设备再次操作会看到&ldquo;会话已在别处登录&rdquo;提示.

**Description:** "修改邮箱、修改密码、登出、多设备单会话机制。"

- [ ] **Step 3: Create 12.3 通知中心**

Primitives: `DocSection`, `DocCallout`

Sections:
- `id="list"` "通知列表" — 1 paragraph: 通知页面是所有系统公告的集合.
- `id="badge"` "未读角标" — 1 paragraph: 侧边栏的通知入口有红点指示有未读通知.
- `id="mark-read"` "标记已读" — 1 paragraph: 点开任何一条通知会自动标记为已读.

**Description:** "通知中心的列表、未读角标和标记已读逻辑。"

- [ ] **Step 4: Create 12.4 提交反馈**

Primitives: `DocSection`, `DocFeatureGrid`, `DocCallout`

Sections:
- `id="entry"` "在哪里反馈" — 1 paragraph: 反馈入口通常在个人中心或账户设置里.
- `id="types"` "五种反馈类型" — `DocFeatureGrid columns={3}`:
  - `{ title: "功能建议", desc: "想到更好用法" }`
  - `{ title: "内容纠错", desc: "发现题目或内容有误" }`
  - `{ title: "界面体验", desc: "用得不顺手的地方" }`
  - `{ title: "Bug 报告", desc: "系统出错或崩溃" }`
  - `{ title: "其它", desc: "其它想说的" }`
- `id="contact"` "联系方式（可选）" — 1 paragraph: 留邮箱或其它联系方式让我们可以回复，不留也可以提交.
- `id="what-happens"` "我们会处理" — `DocCallout variant="info"`: 我们会认真看每一条反馈，但不一定每条都回复.

**Description:** "反馈的五种类型、联系方式字段和反馈流程。"

- [ ] **Step 5: Create 12.5 常见问题 FAQ**

Primitives: `DocSection`, `DocFaqAccordion`

Sections — each section uses `DocFaqAccordion` with 3–5 Q&A entries. Group 1 sentence intro before each accordion.

- `id="account"` "账号与登录" — `DocFaqAccordion` items:
  - `{ question: "忘记密码怎么办？", answer: "在登录页点&ldquo;忘记密码&rdquo;，通过邮箱验证码重设。" }`
  - `{ question: "验证码收不到？", answer: "请先检查垃圾邮件和垃圾箱；如果仍没收到，稍等几分钟后重发。某些邮箱服务商可能会延迟。" }`
  - `{ question: "微信扫码失败？", answer: "请确保微信已登录且网络正常；如果反复失败可以改用邮箱验证码登录。" }`
  - `{ question: "被踢下线了？", answer: "斗学每个账号同一时间只允许一处活跃会话，新设备登录会踢掉旧设备。重新登录即可。" }`

- `id="payment"` "会员与支付" — items:
  - `{ question: "订单支付后没到账？", answer: "通常延迟不会超过 1 分钟。如果超过 10 分钟仍未到账，在账户反馈里提交&ldquo;Bug 报告&rdquo;，附上订单号。" }`
  - `{ question: "订单多久过期？", answer: "创建后 30 分钟内未完成支付会自动过期，需要重新生成订单。" }`
  - `{ question: "可以退款吗？", answer: "数字商品一经发放不支持退款；购买前请确认清楚。" }`
  - `{ question: "终身会员真的永久吗？", answer: "是的，一次购买永久有效，并且每月会继续赠送 15,000 能量豆。" }`

- `id="learning"` "游戏与学习" — items:
  - `{ question: "为什么关卡被锁住？", answer: "免费用户只能玩每个游戏的第一关，其余关卡需要 VIP 会员解锁。" }`
  - `{ question: "学习数据没同步？", answer: "先刷新页面。如果仍不同步，请检查网络或在反馈里告诉我们具体情况。" }`
  - `{ question: "PK 连不上对方？", answer: "指定 PK 需要对方也是 VIP 且当前在线。如果对方已离线可以尝试随机 PK 与机器人对战。" }`
  - `{ question: "为什么 60% 正确率才有经验？", answer: "这是为了保证经验反映的是真实掌握度；60% 是斗学当前的阈值。" }`

- `id="ai"` "AI 与能量豆" — items:
  - `{ question: "能量豆不够怎么办？", answer: "可以等每月赠送（月/季/年会员 10,000 豆，终身 15,000 豆）或直接充值。" }`
  - `{ question: "AI 生成失败？", answer: "消耗的能量豆会自动退还。如果频繁失败，可以换个关键词或稍后再试。" }`
  - `{ question: "能量豆会过期吗？", answer: "月度赠送的豆每月会清零；自己购买的豆不清零。" }`

- `id="tech"` "技术与兼容" — items:
  - `{ question: "支持哪些浏览器？", answer: "Chrome、Safari、Edge 最新版体验最佳。旧版浏览器可能出现兼容问题。" }`
  - `{ question: "图片上传失败？", answer: "请确认文件小于 2MB，格式为 JPEG 或 PNG。" }`
  - `{ question: "页面卡住不动？", answer: "先尝试刷新；如果仍不正常，请在反馈里附上浏览器版本和具体场景。" }`

**Description:** "五大常见问题分区：账号 / 会员 / 学习 / AI 与豆 / 技术。"

- [ ] **Step 6: Update registry**

Edit `src/features/web/docs/registry.ts`:

1. Add imports:
```ts
import { UserCog } from "lucide-react";
import ProfileEdit from "./topics/account/profile-edit";
import Security from "./topics/account/security";
import Notices from "./topics/account/notices";
import Feedback from "./topics/account/feedback";
import Faq from "./topics/account/faq";
```

2. Append new category:
```ts
{
  slug: "account",
  title: "账户与帮助",
  description: "资料设置、账号安全、通知、反馈，以及常见问题合集。",
  icon: UserCog,
  accentClass: "border-slate-200 bg-slate-50 text-slate-600",
  topics: [
    {
      slug: "profile-edit",
      title: "个人资料",
      description: "可编辑的资料字段、头像上传、用户名为什么只读。",
      Component: ProfileEdit,
    },
    {
      slug: "security",
      title: "账号安全",
      description: "改邮箱、改密码、登出、多设备单会话机制。",
      Component: Security,
    },
    {
      slug: "notices",
      title: "通知中心",
      description: "通知列表、未读角标和已读机制。",
      Component: Notices,
    },
    {
      slug: "feedback",
      title: "提交反馈",
      description: "五种反馈类型、联系方式和流程。",
      Component: Feedback,
    },
    {
      slug: "faq",
      title: "常见问题",
      description: "账号、会员、学习、AI、技术五大分区的常见问答合集。",
      Component: Faq,
    },
  ],
},
```

- [ ] **Step 7: Typecheck**

Run: `cd dx-web && npx tsc --noEmit 2>&1 | tail -20`
Expected: zero output.

- [ ] **Step 8: Commit**

```bash
cd dx-web && git add src/features/web/docs/topics/account/ src/features/web/docs/registry.ts && git commit -m "feat(docs): write account category (incl. FAQ)"
```

---

## Task 23: Final verification — build, lint, manual route check

With all 48 topics registered, every page in the help center should prerender and render correctly. This task catches anything that drifted during content writing.

- [ ] **Step 1: Run full production build**

Run: `cd dx-web && npm run build 2>&1 | tail -60`

Expected: `✓ Compiled successfully`, no errors, and the build output lists static `/docs`, `/docs/[category]`, `/docs/[category]/[topic]` routes. The count of generated paths should be **61** (1 landing + 12 category indexes + 48 topic pages).

If any error appears, read the message, fix the specific file it mentions, and re-run. Do NOT commit anything here — the plan already committed each task separately.

- [ ] **Step 2: Run lint**

Run: `cd dx-web && npm run lint 2>&1 | tail -30`

Expected: zero errors. Warnings are OK but should be investigated.

- [ ] **Step 3: Start dev server for manual verification**

Run: `cd dx-web && npm run dev`

Leave it running during the following checks.

- [ ] **Step 4: Verify landing page**

Visit `http://localhost:3000/docs`

Check:
- Hero with "斗学帮助中心" title + subtitle + two CTA buttons
- "全部分类" heading
- 12 category cards in a responsive grid, each with colored icon, title, description, "N 个主题 →"
- Clicking any card navigates to `/docs/[category]`

- [ ] **Step 5: Verify category index page**

Visit `http://localhost:3000/docs/learning-modes`

Check:
- Breadcrumb: 文档 / 三种学习模式
- Category header with rose-colored `Swords` icon + H1 "三种学习模式"
- 5 numbered topic rows (1–5), each with title and description, hover state shows teal highlight + arrow slide
- Each row links to the topic page

- [ ] **Step 6: Verify primitive-heavy topic page**

Visit `http://localhost:3000/docs/learning-modes/pk-mode`

Check:
- Breadcrumb: 文档 / 三种学习模式 / PK 对战模式
- H1 "PK 对战模式" + lead paragraph
- At least 5 H2 sections rendered with anchors
- `DocCallout` warning box "仅 VIP 可用"
- `DocCompareTable` comparing random vs specified
- `DocKeyValue` showing robot difficulty specs
- `DocSteps` walkthrough of invite flow
- `DocFlow` showing pending → accepted → declined
- Prev/Next footer at the bottom (prev: 单人闯关模式, next: 小组共学模式)
- Right-rail TOC visible on xl screens (≥1280px width) — scroll to verify scroll-spy highlights active H2

- [ ] **Step 7: Verify FAQ topic**

Visit `http://localhost:3000/docs/account/faq`

Check:
- 5 sections (账号 / 会员 / 学习 / AI / 技术)
- Each section has a `DocFaqAccordion` — click a question to expand the answer, click again to collapse

- [ ] **Step 8: Verify 404 handling**

Visit:
- `http://localhost:3000/docs/nonsense` — expect Next.js 404 page
- `http://localhost:3000/docs/getting-started/nonsense` — expect Next.js 404 page

- [ ] **Step 9: Verify sidebar highlight**

While on any topic page, verify the left sidebar:
- The current topic is highlighted with teal background + teal text + filled left accent bar
- The current category's title is highlighted in teal
- Other categories/topics are in muted slate tones

- [ ] **Step 10: Verify prev/next reading order**

Starting from `/docs/getting-started/what-is-douxue`, click Next repeatedly and verify it walks through all 48 topics in category order (not skipping, not repeating). The final Next should be on topic 12.5 (FAQ); after that, Next should be hidden/disabled.

- [ ] **Step 11: Verify mobile drawer**

Resize browser to <1024px wide.

Check:
- Left sidebar hides
- Right TOC hides (was only xl+)
- A sticky top strip appears with a "目录" button
- Clicking "目录" opens a Sheet drawer from the left containing the full sidebar
- Clicking a topic in the drawer navigates and closes the drawer

- [ ] **Step 12: Verify LandingHeader /docs link still works**

Visit `http://localhost:3000/` (landing home). Click "文档" in the top nav. Verify it lands on `/docs`.

- [ ] **Step 13: Stop dev server**

Kill the running `npm run dev` process.

- [ ] **Step 14: Final sanity — git log**

Run: `git log --oneline main ^origin/main | head -30`

Expected: See commits from Tasks 1–22 in reverse chronological order (Task 22 most recent). Count should be 22. No uncommitted changes.

Run: `git status`

Expected: `nothing to commit, working tree clean`.

If clean, the implementation is complete.

---

## Post-implementation follow-ups (not part of this plan)

The following are intentionally out of scope for this plan but can be considered later as follow-up work:

1. **Hall sidebar 帮助中心 link** — Requires updating the backend hall menu service at `dx-api/app/services/api/hall_service.go` (or equivalent) and re-running the hall menu API. A small change, but crosses the frontend/backend boundary and should be its own plan.
2. **Real footer links** — The current footer uses `<span>` mock links. Making them real `<Link>` elements (including a `/docs` link under 斗学团队 or 斗学产品) is a broader footer refactor.
3. **Docs search** — Client-side fuzzy search over topic titles + content. Non-trivial (requires index generation), deferred to v2.
4. **Real screenshots** — Replace `DocPseudoUI` blocks with captured screenshots of the running app. ~20–30 screens to capture.
5. **Topic feedback** — Thumbs up/down on each topic page, wired to the feedback API.
6. **Page-view analytics** — Track which docs topics are viewed most.
7. **OG images per topic** — Generated via `@vercel/og` or similar.

None of these block this plan from shipping.
